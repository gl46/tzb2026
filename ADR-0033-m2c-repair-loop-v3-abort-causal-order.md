# ADR-0033 — v3 首轮中止的因果顺序更正,与 v4 重跑条件

- 日期:2026-09-02
- 裁定人:tzb-fe(协调会话)
- 执行线:isolate-prereg-loader-recovery(自修复闭环,node2:18767)
- 前序:`ADR-0032-m2c-repair-loop-v2-limit-cycle.md`(已冻结不可变,sha256 前 16 位 `dba870d09b5027cc`,13867 字节)
- 本文存在的理由:ADR-0032 已冻结,其"冻结声明"一节对中止原因的表述有误。**按该冻结声明自己定下的规矩,更正写入后继文件,不回改 ADR-0032。**

## 更正:两个故障,顺序不能倒

ADR-0032 的冻结声明把 v3 首轮无法完成归因于 tzb-fe 在运行期间修改被绑定文件所致的 source drift。**该归因顺序错误。** 由执行线直接读第 26 条 record 后确认(state gen587/588),正确因果为:

**故障一(首要,先发生)—— 预登记内的正常中止路径**

- 位置:`single-07-missing-safety-predicate` attempt-01
- `disposition = INFRASTRUCTURE_FAILURE_ABORT`
- `code = MODEL_ENDPOINT_REQUEST_FAILED`
- `message = TimeoutError: model request exceeded 60.000s`
- `stage = http_request`,`model_request_sent = true`,`model_output_observed = false`
- `elapsed_s = 60.0087`

**整轮在此刻已按预登记 ABORT。** 这是冻结 runner 的设计行为,不是缺陷。诱因是 node2 共享 A100 被第三方任务(PID 123120,3010 MiB)占用后解码被饿到 0.9 tok/s。

**故障二(次生,后发生)—— 由 tzb-fe 造成**

runner 在中止后仍执行 post-run frozen-source check,发现预登记绑定的 ADR digest(`6bdb33f22f4bab2f` / 12253B)与磁盘上的实际文件(`e64431f405423284` / 13085B)不符,raise RuntimeError,**导致连 abort result 工件都未能落盘**。

漂移由 tzb-fe 在该轮运行期间修改 ADR-0032(`8/22` → `9/23`)造成。

## 两者的责任划分,不得混淆

| | 故障一 | 故障二 |
|---|---|---|
| 性质 | 基础设施(GPU 争抢) | 治理流程(在活的绑定文件上动手) |
| 责任 | 无人,外部争用 | tzb-fe |
| 后果 | 整轮 ABORT,符合预登记 | abort 工件无法落盘 |
| 是否影响科学结论 | 否(无 headline) | 否(无 headline) |

**把故障二写成首要原因,等于把一次正常的、预登记之内的基础设施中止,说成治理事故。** 两者都要披露,但顺序和归属必须准确。tzb-fe 初次表述时过度归因于自身,同样是不准确,已在此更正。

## v4 重跑条件(承接 ADR-0032 §5,不变更任何科学参数)

1. fresh 预登记(v4)、fresh evidence 目录、fresh result 路径;**不得沿用 v3 已污染路径,不得把中止轮描述为"完成"**。
2. 预登记同时绑定 **ADR-0032**(`dba870d09b5027cc` / 13867B,已 chmod 444)与**本文件 ADR-0033**。两者均不可变。
3. **ADR-0032 §5 的全部科学参数不变**:六个采样参数、§5.2 逐项相同清单(含 60s 超时)、§5.3 先冻后输出、§5.4 判据、§5.5 证伪不作能力估计。本文件不修改其中任何一项。
4. **超时保持 60s。** 不为规避 GPU 争抢而变更冻结参数——那会把单变量变成双变量。
5. **起跑前先探端点往返延迟。** 探测规格如下,**不得消耗 case,不得进入实验证据**:

   - **只读部分**:`GET /v1/models` 健康检查。免费,不计入任何分母。
   - **代表性请求**:**一次**,使用与实验相同的 schema 与请求控制(含 60s 超时),但**其 payload 不得取自那 24 个 case 中的任何一个**,输出**不得写入实验 evidence 目录**。单独记为 latency receipt(create-once),与实验工件分离。
   - **判据**:代表性请求端到端 **≤ 30s → 放行开跑**;**> 30s → 不开跑,等待后重探**。
   - **30s 这个阈值的依据**:v2 在无争抢时单次 attempt 的 `elapsed_s` 约 24–35s(如 `single-01` attempt-00 为 34.49s),而冻结超时是 60s。取 30s 意味着对超时留约 2× 余量;高于 30s 说明卡已被争用到没有安全裕度,此时开跑大概率再次整轮 ABORT 并白烧一套 fresh 路径。
   - **重探次数不限**,每次都是同一个单发探测;等待期间不占用任何实验资源。**不得因为多探几次就放宽 30s 阈值。**
6. 最终分析必须披露 v3 首轮的完整运行史:26 个 attempt / 7 案、上述两个故障及其顺序、未发布任何 headline。

## 沉淀的两条硬规矩

- **gen583** —— 禁止对运行中的证据目录取终值。(今晚犯两次:`0/12`、`8/22`)
- **gen586** —— 禁止修改运行中被绑定的冻结文件。(今晚犯一次,即本文故障二)

同源:在活的东西上动手。前者读,后者写。

## GPU 现状(用户已裁,无需任何操作)

node2:vLLM(PID 35678)76354 MiB **静态占用**——启动时已预分配 21.29 GiB KV cache,不动态增长;第三方任务(PID 123120)3010 MiB;剩余 1761 MiB。

用户裁定不让出份额。**该裁定不要求任何针对第三方进程的动作:不 kill、不清理、不重启。** 我方份额在启动时即已 claim,无需争抢即可保持。

## 冻结声明(2026-09-02,tzb-fe)

**本文件自此不可变,chmod 444。** 与 ADR-0032 同规矩:此后任何更正写入 ADR-0034 起的后继文件,不回改本文件。v4 预登记可安全绑定本文件 digest。
