# Prerequisites

System dependencies are intentionally not installed by this goal. On the selected Ubuntu 24.04
simulation host, a human should review the printed Jazzy/Harmonic plan, install through approved
administrative process, then source the selected ROS environment. Docker/NVIDIA container support
is optional and not a P0 precondition. No model download is required.
