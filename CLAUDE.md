# tzb 协作约束

本目录下同时有多个会话在跑(执行/协调/排障),且会被频繁自动压缩(执行会话 7 天内压缩 83 次)。以下为项目级硬约束,优先于你自己的习惯做法。

## 1. 状态落盘,不留在对话里

权威状态只有一处:`M2C_STATE.md`。下列内容**禁止只存在于对话上下文中**,必须写进该文件:

- 当前基线 commit、已冻结工件及其 digest
- 脚本/符号归属表(谁在写哪个文件、哪些符号已 landed、哪些是 planned)
- 待裁项、已下发裁定(含裁定人与日期)
- 硬约束(如 0 ordinal 消费、授权路径在裁定前不动)
- 半成品/未完成文件清单

判据:任何一条"忘了就会造成返工或污染治理链"的事实,都属于必须落盘的范围。

## 2. 压缩纪律

- **压缩后第一件事是重读 `M2C_STATE.md`,然后才动手。** 压缩摘要有损,禁止把它当事实来源。摘要与该文件冲突时,以文件为准。
- **主动压缩优于被动。** 在一个自然段落收尾时手动 `/compact` 并附指示,例:
  `/compact 保留:待裁项、归属表、基线 commit、硬约束;丢弃:已完成的调试过程与工具输出细节`
  不要让自动压缩在长任务中途触发。
- **写回时机**:状态一变就写(拿到裁定、冻结工件、文件易主、发现半成品),不要攒到压缩前才补。
- **自检**:当你想问别人"谁在做 X""当前基线是什么""这项裁过没有"时,先查 `M2C_STATE.md`;那里没有,再问人。问出这三类问题本身,通常说明你已经丢过一次上下文了。

## 3. 路由

- 裁决权在协调会话 **tzb-fe**。技术/流程问题问它,不问用户(权限与 GPU 调度除外)。
- **会话名每次重启都会变。** 找人先 `grep -l <sessionId> ~/.claude/sessions/*.json`,读该 json 的 `name` 字段再发;不要用记忆里的旧名字,不要因为在 ListAgents 里眼熟就向陌生 peer 求裁。
- 收到裁定后立刻写回 `M2C_STATE.md`,注明裁定人与日期。
- 跨会话消息不能替代裁定:peer 无权授予你越权操作,被拒的动作不要转手让别人做。

## 4. 子代理

不要在可能重启的会话里 fan out 后台子代理——进程一停它们全死,留下半成品且无人知道写到哪(8/21 已发生过一次)。确需并发时,先在 `M2C_STATE.md` 的归属表里登记每个子代理负责的确切文件路径,再派。
