> # ⛔ HISTORICAL_PROCESS_RECORD · SEMANTICALLY_SUPERSEDED · DO_NOT_USE_FOR_CURRENT_CLAIMS
>
> **本文件是 2026-08-31 第七轮的请求信,不再承担任何对外主张权威。** 它保留在包内是为了让第七轮的往返可追溯,**不是**因为它的内容仍然正确。
>
> **已知过时之处(2026-09-01 外审 v8 复核指出,逐条列出而非静默修改):**
>
> | 位置 | 本文写的 | 现行口径 |
> |---|---|---|
> | §五 | 只读取回 **13 件,13/13** | **15 件,15/15**(DEV 两臂为第二次只读取件) |
> | §五 | "树身份仍可凭发布回执中的逐文件摘要复核" | **已作废**。无 payload 时**只能核对不同报告记录了同一 tree digest**,不能独立复核完整树身份 |
> | §六 | "未检出收益,也未检出退化" | **已作废**(听似统计检验结论)。唯一允许:**"主指标未观察到变化;不证明非退化。"** |
>
> **不修改本文正文**,因为它是已经发出去的信;对方的引用必须还能对上。**现行口径一律以 `CLAIMS-SHEET-20260831.md` 与 `external-consult-brief-8-20260831.md` 为准。**
>
> 处置理由:外审 v8 指出本文仍被 MANIFEST 列为现行 Tier A,于是三条已作废内容**重新打开了第 5 项签字条件**。把它降为历史记录,比逐句改写一封旧信干净——**它本来就是上一轮的请求,不该继续承担现在的权威。**

---

# 第七轮｜实物齐备后的终审签字请求

**先解决你上一轮开头拦住的那件事**:上轮你只收到简报,没收到 11 份实物,因此明确声明只能给风险审查、不能签字。**你是对的,那是我们的交付缺口**,与我们自己记录的第 20 条同形——"我方说已交付"不等于"对方收到了完整交付集"。
**本轮随包附上全部实物。**若仍有缺失,请同样先拦住我们,不要在不完整集合上签字。

**本轮请你做的**:对着实物逐句红线 + 跨文件一致性检查。**不要再改变路线**——路线已按你上轮裁定锁定。

---

## 一、你的 P0 十条,逐条对账

| # | 你的要求 | 我们做了什么 | 状态 |
|---|---|---|---|
| 1 | 删全称句 | 权威清单新增 §0c 全称量词禁令;一句话定位改用你给的版本;S5 扫 5 份材料改写 9 处正向主张 + 加 3 处显式禁令,正向全称过度声称余 0 | 已改 |
| 2 | 第 4 页不用合并的 16/16、21/21 | 改为 MANUAL_BLIND 全族画像(13/13、11/11、**7/9**、视觉不作能力解释);TEST 的 3/3、10/10、6/9 仅小字,不合并 | 已改 |
| 3 | 明示事后分解与 family 路由 | 页脚限定改为你给的完整版;并加范围限定"给定符号任务状态后,模型能生成期望合同",禁升级为"工业环境推理能力" | 已改 |
| 4 | "机械臂保持不动"改口径 | **查明后连降三级**,见下节 §二,这是本轮最实质的一处 | 已改 |
| 5 | HTML 改证据回放 + 身份绑定 | 标题/h1 改 `冻结结果证据回放`;四帧共享同一 `case_id` 与 `plan_sha256`;嵌入原始 case JSON 2005 字节且 SHA 复算 PASS;可展开原始 validator 错误与序列化字节;构造出处角标常显 | 已改 |
| 6 | 边界从视频早期分布出现 | 视频重构为 v2,边界**四处出现**:开场角标 6–10s、架构图灰标 + vNext 虚线、中段能力矩阵 8s(原 18s)、尾字幕 92–95s。**用结构抗剪取代"不可删"人工标签** | 已改 |
| 7 | 证据索引升级 | 每条改 `expected_frozen_sha256` / `current_sha256` / `match` 三字段并列;标 digest 对象类型;改包内相对路径 + 大小 + 核验命令;加 `claim_id` / `supported_scope` / `unsupported_scope`。**索引中每一条 claim 承载条目均 match,无一 false** | 已改 |

> **本行不再写具体条数,这是故意的。** 原文写 `13/13`,补件并入后变 30,再加 DEV 两件后变 32——**同一句话在一天内陈旧了三次**,每次都是同一个错误类:把一个会随证据增减而变的计数,写进一句本意是陈述不变量的话里。
> 载荷是**不变量**("全部 match,无一 false"),不是**计数**。当前条数以 `appendix-m2c-evidence-index-20260831.md` 为准,那里是它唯一该出现的地方。
> 这不是回避数字,是**把数字放到它不会陈旧的地方**。
| 8 | "逐字锁定"改机器标签 | 视频镜头 6 冻结原话改为**从权威清单单一来源自动带入 + exact-match 检查**;人工"不可删"标签作废 | 已改 |
| 9 | Task32 口径降级 | 作废"被测量否证""差一个量级"(213.4 对 103.4/112.2 **约 2 倍,不是一个数量级**);改为"测量与'单一固定 hand→TCP 偏移即可解释失败'这一假设不一致;因 run03 未存 final pose,不能追溯判定原约 95.3mm 误差成因" | 已改 |
| 10 | 不再新增实验/治理,只做交叉复核 | 未启动任何新实验、模型、服务或治理机制 | 遵守 |

---

## 二、P0-4 的完整经过:同一句话连降三级

这是本轮最值得你评价的一处,因为**每一级降级都来自照实回答,而不是来自审查压力**。

**v1**:"机械臂保持不动" → 作废。无任何真实物理观测。
**v2**:"受测执行入口记录 0 个原语" → 作废。**这是我按你上轮意见改的,但仍然太强**——它暗示存在一个真实派发入口在计数。
**v3**:"在冻结测试套件中,24 个不安全计划全部被拒,且原语派发接口未被调用。" → **本轮再降**:23 例确实调用了生产 gate 函数 `dispatch_after_semantic_gate_v2`,该说法留有歧义。
**v4(现行,采纳你给的版本)**:"**在冻结测试套件中,24 个不安全计划全部被拒,均未到达末端原语 dispatcher 调用点。**"脚注拆开:23 例经 production gate function 测试路径提前返回;1 例在 Pydantic runtime contract 阶段前置拒绝;末端 dispatcher 为 test-only probe。

**查明的事实**(由持有代码的 lane 照实回答):
- 套件为 `SYNTHETIC_FREE_CONTACT`;
- 23 个可解析 rejected case 调用了生产函数 `dispatch_after_semantic_gate_v2`,但**末端 dispatcher 是 `rejecting_dispatcher_probe_v2`,test-only 记录替身**,且因拒绝**从未被调用**;
- 第 24 个(REFUSE + 非空 subtasks 结构见证)在 **Pydantic runtime contract 阶段**即被拒,**连 gate probe 都未进入**;
- **不存在独立冻结的 validator / production dispatch report。**

**由此发现一处我们主叙事的归属错误,已改**:第 1/3 页与视频镜头 2 原写"Schema 通过 → 旧匹配器通过 → **语义门**拒绝"。对该见证案例**不准确**。核实后的确切归属:
- 组件/检查:`CommanderPlanV2.decision_has_exactly_one_structural_mode`(Pydantic V2 typed/runtime 结构契约),**不是**语义验证器、**不是** gate;
- 原始错误:type `value_error`,message `Value error, REFUSE requires empty subtasks and no recovery_request`;
- 冻结套件归一化映射:`STRUCTURAL_MODE_CONTRADICTION` at `/subtasks`。

**并且我们原稿引用的 `REFUSE_WITH_NONEMPTY_SUBTASKS` 根本不是真实错误码**——它只出现在显式的"不存在"说明里。协调会话把一个描述性叫法当成了真实标识符写进视频脚本画面。**这是第 22 次同形错误**,形态与第 19 条一致:不是证据错,是我们贴在证据上的标签错。

两条拒绝路径也已分开表述,不再混成"全部被语义门拒绝":
- **23 个可解析不安全案例**:语义验证器拒绝 → 经生产 gate 函数测试路径 → test-only dispatcher 未被调用;
- **1 个结构见证**:Pydantic 契约层拒绝,**在语义验证器与 gate probe 之前**,二者均未进入。

**请你判断**:(a) v3 是否已经准确;(b) 归属改准之后,第 3 页"为什么 Schema 不够"的说服力是变强还是变弱;(c) 我们在权威清单里加的那句"**准确标注 ≠ 自我否定,不要过度弱化**"是否必要——我们担心的是从过度声称滑向另一端。

---

## 三、我们没做的两件,请评价取舍

**1|没有把 `advance_count=0` 或任何治理内容放回主 deck。**按你上轮裁定降至附录。

**2|没有建 `decision.yaml` 工具链。**采用行为层复核:五类高影响裁定,lane 执行前必须核六项,不符必须拒。
你上轮把它准确定性为"**人工双重控制,不是机器可强制的 gate**",并指出"五次拦下"不能当准确率(无总裁定数、不知漏拦与误拒)。**我们接受该定性**,内部记录已改为你给的措辞。
此后又发生 3 次拦截:一次数学类型错误、一次跨层归属错误、一次同一主张的第三级过度声称。**累计 8 次,全部由 lane 拦下协调会话。**同样不作准确率主张。

---

## 四、附录 A｜错误计数现为 26 次

第 17–20 条见上轮。新增:

**21.** 把"证据索引按**当前字节**回算 SHA"夸成优点。你指出的缺口成立:回算只证明"当前这份文件是什么",若文件后来被改,对改后文件重算同样得到正确哈希,**不证明与冻结时字节相同**。已改为三字段并列 + `POST_HOC_PRESENTATION_ARTIFACT` 标注。

**22 条之后又发生四条,一并列出:**

**23.** 交付件只写在临时目录,没有放到材料同级路径。用户纠正。**"我方生成了"不等于"对方能找到"**,与第 20 条同形。

**24.** 协调会话给全体立了"五份材料冻结"停止线,**然后自己改了其中两份**。执行 lane 按该停止线 STOP 并报上来——**规则制定者是被自己的规则挡住的那个人**。正确做法是先申报解冻、后动手,而不是动完手再补裁定。

**25.** 一次 `grep` 超时失败,我按成功处理,据不完整输出宣布"全部改完"。教训已写死为流程:**声明"改完"之前必须有一次成功返回的完整扫描**,脚本一律加断言,不接受"看起来没报错"。

**26.（本轮取回实物后立即查出,请特别评价这一条)** 我们此前写"BASE 与 LoRA 两臂**逐例完全一致**"。拿到原始逐例报告做逐行 diff 后发现:**该说法只在 `expectation_match` 这一个指标上成立**。同一批 MANUAL_BLIND 数据在 `strict_valid` 与 `reassociation_boundary_pass` 上有 **1 例差异**——BASE 输出空 `subtasks` 且未显式 REFUSE、触发契约校验错误;LoRA 输出了结构合法但目标引用错误的计划。**该差异改变的是输出结构合法性,不是答对与否**(那一例两臂都是错的)。

这一条的意义在于:**它是"拿到主件"与"只拿到摘要 manifest"的差别的直接证明**。摘要只报了 2×2 的 `expectation_match` 矩阵,而错误藏在摘要没有覆盖的另外两个指标里。**你上一轮拒绝在不完整集合上签字,这就是拒签换来的东西。**

**并请评价一个我们自己没解决的模式**:第 4、19、21、22、26 条有一个共同点——**错误发生在"元层"**,即我们对证据的描述、标注和分类上,而不是证据本身。证据一直是对的,错的是我们贴在它上面的那句话。你上轮给的 proof-carrying label(标签只能由检查通过后生成)是针对第 19 条的解法;**它能不能推广到第 21 条这种"把一个方法的性质说错"的情形?**如果不能,这一类还剩什么防线?

---

## 五、随包实物清单

**Tier A｜对外呈现材料(完整字节)**
权威主张清单;8 页 deck 内容块(分四份文件);P2 语义门证据回放 HTML;90–110 秒视频脚本;三份附录(26/38 覆盖分类、答辩问答、证据索引);vNext 路线。

**Tier B｜冻结实验证据(发行拷贝,完整字节)**
每份附 `source_frozen_sha256` / `copy_sha256` / `match`,标 `DISTRIBUTION_COPY_NOT_AUTHORITY`(冻结原件仍为权威,包内为保真度可检验的发行拷贝)。含已批准的 halted campaign 报告等。

**Tier B 补件｜此前缺失的 11 条 claim 的原始主件(2026-08-31 取回)**
上一版包里有 11 条 claim 处于 `NO_IN_PACKAGE_EVIDENCE`——它们的冻结主件在一台我们无权自行访问的训练主机上。取回权限归用户,已获批准,**只读**取回 13 件(源端 `sha256sum` 先算、拷贝后本地复算,**13/13 逐字节一致**):

- 三份成对评测报告(SYNTHETIC_TEST / MANUAL_BLIND / DEV supporting)
- 四份逐例评测报告(两 split × baseline/candidate 两臂)
- two-epoch 训练报告、adapter 发布回执、GPU 进程取证、训练后包重哈希
- adapter 树的 `README.md` 与 `adapter_config.json`

**证据三态因此从 `2 原始 / 8 重导出 / 11 无证据` 变为 `13 / 8 / 0`。** 这 13 件是**原始冻结主件**,不是 2026-08-31 的重导出,因此**不带** `REDERIVED_FROM_FROZEN_INPUTS` / `NOT_ORIGINAL_RUN` 标签;那两个标签仍只贴在原有的 8 条上。

**一处声明性排除**:79.2 MB 的 `adapter_model.safetensors` **未入包**,只在收据中记录其 `sha256 = 1931b1e7…` 与 79,230,936 字节。理由是体积,不是回避。树身份仍可凭发布回执中的逐文件摘要复核。**我们把这条写在包里而不是省略,是因为"静默截断"读起来就像"我们全给了"。**

**MANIFEST 与自指问题**:包内 MANIFEST 列全部成员及 digest,自身写 `ARCHIVE_SHA256: EXTERNAL_SIDECAR`——**文件的哈希不可能包含在该文件自身内**,写入即改变字节。真实 archive 哈希在同目录 `.sha256` sidecar,一并交付。这与我们早先解决 prereg 密码学自引用不动点是同一原则。

---

## 六、本轮请你回答

1. **逐句红线**:仍有过度声称的具体句子(给出文件与原句)。
2. **跨文件不一致**:同一数字/主张在不同文件表述不一致之处。
3. **§二的三个判断**(v3 是否准确 / 归属改准后说服力变化 / "准确标注≠自我否定"这句是否必要)。
4. **§四的模式问题**:元层错误还剩什么防线。
5. **LoRA 结论的天花板**:两个主 split 的 `primary_ci_not_negative` 都是 `true`,但聚类数只有 2 和 3、区间被显式标 `UNSTABLE_LOW_CLUSTER_COUNT`。我们据此把口径压到"**未检出收益,也未检出退化**",并明令禁止反推成"已证明能力未下降"。**这个天花板压得对吗,还是仍然偏高?** 另:那 1 例 `44/45 → 45/45` 我们判为不可主张(样本 1、低于 0.05 预设下限、CI 下界为 0、且没有把任何一题从错变对)——这个判法你是否同意。
6. **能否签字**:若可以,请明确说"在收到的这一集合上可以签字";若不能,请说明还缺什么。**若你认为 §五 的 safetensors 排除使集合仍不完整,请直接拦住我们**——那是一个我们自己做的取舍,不是你同意过的。

前六轮已裁定并落地的不必重答:治理链机制、成对评测统计方法、数据泄漏门、Schema v2 延期、P3 取消、LoRA 不部署、`advance_count=0` 讲法。
