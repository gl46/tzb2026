# 第五轮外部咨询 · 挑战杯 XH-202607(2026-08-31)

**这是第五轮,也大概率是最后一轮。** 请**优先反驳我们**——前四轮价值最高的产出全部来自你指出我们错了(二轮你预判"LoRA 大概率打平",落实了;三轮你指出"视觉任务不是贡献小,而是统计上不可识别",改写了路线;四轮你把我们一串自查错误归因为"有 content addressability,没有 semantic addressability 与 referential integrity",已拿去改流程)。

**本轮处境与前四轮都不同:四条主线现在全部为负或缺席。** 我们不是来找安慰的,是来问"在这个已经定死的事实集合上,能拿出的最强提交是什么"。如果答案是"拿不出",请直说并给止损方案。

**时间线**:今天 8/31 凌晨。**代码冻结 9/3**,材料 9/3–9/5,**提交 9/5**。剩约 3 天工程 + 2 天材料。**赛题名**:"工业环境下物体感知识别与指令交互型智能体研发",评委非机器人 / 非 ML 专业。**未脱敏**(用户 8/30 决定):以下主机名、绝对路径、commit、SHA-256 均为真实值,照原样给出,便于你独立核对内部一致性。

---

## 0. 前四轮速览(不必重读历史)

| 轮次 | 问了什么 | 结论 / 我们做了什么 |
|---|---|---|
| 一 | 四个方法论难题:抓取基线偏差、小数据微调、近重检测、阴性叙事 | 判:ICL 做主路径、LoRA 做受控实验,"只有 LoRA 稳定超过调优后的 ICL 才作为亮点"。已照做。 |
| 二 | 带真实数字回来:微调与 few-shot 打平了,五天内怎么救 | 判:不为出正数扫超参、不启动 3000 episode 确认实验;打平要讲成有信息量的结论。已照做。 |
| 三 | 附完整代码/配置/数据/证据包,"我们整体判断链哪里想错了" | 三项核心纠正:(a) 抓取线**不是** 0/12,是 5 个有效结果后停机 + 7 个 ordinal 未执行,下措辞禁令;(b) 视觉任务**不是**"贡献小",是**统计上不可识别**;(c) 现 evaluator 只验字段出现,是"最低期望匹配器"。全部落地。 |
| 四 | 六件:叙事预注册 / P3 设计评审 / deck 页面级结构 / 阴性承载上限 / Schema v2 动不动顶层 / 构造性 TOCTOU 归哪层 | 已落地:边界"最多一页专门讲,其余分散到对应能力旁";Schema v2 顶层**裁定延期**;自查错误根因 = **有 content addressability,没有 semantic addressability 与 referential integrity**。**P3 后被我们自己按硬边界取消**(§1.3),不是你的评审不成立。 |

**本轮五问,与前四轮无重叠。凡前四轮已裁的,本轮不再问**(清单见附录 B)。

---

## 1. 本轮处境:四条主线全部为负或缺席

请把这一节当成**已定死、不可再改的事实集合**来读——9/3 冻结前我们能改变的只有 §1.5 的 Task32 一项。

### 1.1 抓取 campaign —— 已按预注册停止规则转诚实 halted

**冻结账目**(机器可读:`/private/tmp/m2c-r13239-task31-ordinal05-halt-accounting-v1.json`):

```json
"current_valid_results": 5,          "successful_valid_results": 0,
"physical_dispatch_attempts": 1,     "canonical_claim_present": false,
"canonical_terminal_present": false, "valid_campaign_outcome_added": false,
"failure_class": "HARNESS_IMPORT_ENVIRONMENT_FAILURE_BEFORE_CLAIM",
"runner_returncode": 1,              "ordinal05_redispatch_permitted": false,
"remaining_unexecuted_ordinals": [6, 7, 8, 9, 10, 11],
"status": "HALT_HARNESS_FAILURE_NO_REDISPATCH"
```

**发生了什么**:ordinal05 于 **08/30 23:46** 按 final plan v5 **单次物理派发**(硬时限 08/31 02:00 前)。容器 control 面六个文件齐全(dispatch/result/start receipt + runner.pid + stdout/stderr),但 worker 启动后 runner 在写 claim 之前因缺 `PYTHONPATH` 抛 `ModuleNotFoundError: No module named 'm2c'`,rc=1。**claim05 与 terminal05 均未写,因此未形成 canonical outcome。** 物理派发真实发生,只是没产出可记账的结果。

**为什么不重试**(三条独立依据,任一单独成立):(1) 冻结 auditor 的 harness 终态名字面即 `HALT_SUCCESSOR_R3_HARNESS_OR_CLOSURE_INVALID_NO_RETRY`;(2) 协调在点火前预注册的替代停止规则(点火后任何非"协调裁定规格错误"的结构矛盾直接 halt,不再修);(3) **派发后重打即重试语义**——预注册协议禁止。**最终账目**:有效结果 **5**,成功 **0**,`statistical_claim = null`;ordinal05 一次物理 attempt 但**无 canonical outcome**;06–11 共**六个未执行**。

**关键事实(问题五的全部依据)**:在开火之前,冻结 auditor 已零消费枚举剩余 7 发的全部 **2^7 = 128** 个结果后缀,逐个走**真实判定路径**(禁止重实现)。结果(`/private/tmp/m2c-r13239-task33-reachability-summary-v1.json`):

```json
"advance_count": 0, "advance_reachable": false,
"primary_reason_counts": { "X_LE_3_FLOOR_FAIL_RETURN": 64,
  "X_GE_4_RETURN_FOR_STRATUM_STRUCTURE": 51, "X_4_TO_5_THIRD_REGIME_RETURN": 12,
  "FLOOR_STRATUM_REPRESENTATION_FAIL_RETURN": 1 }
```

即:**`ADVANCE_TO_KEY_FREEZE` 可达数 = 0**,128 个后缀无一能到达我们最想要的终态。原因是分层:2×2 四层,配额 `EXTRAPOLATED_ANCHOR__ESTABLISHED` 4(ord 0–3)/ `..._NEW_TERRITORY` 2(ord 4–5)/ `MEASURED_ANCHOR__ESTABLISHED` 4(ord 6–9)/ `..._NEW_TERRITORY` 2(ord 10–11)。ord 0–3 已跑满且 0/4,**最小层成功率永久锁死 0.000**;而运行前冻结的代码写着:

```python
PROBE_COUNT = 12
EXISTENCE_FLOOR_FRACTION = 0.30            # → 最低 4 次成功
EXACT_BINOMIAL_N, EXACT_BINOMIAL_P0, EXACT_BINOMIAL_ALPHA = 12, 0.75, 0.05
THIRD_REGIME_MAX_SUCCESSES = 5
SEVERE_INTER_STRATUM_DIVERGENCE_THRESHOLD = 0.75
```

任何足以"不拒绝 p₀=0.75"的成功密度(X≥6),必然把层间成功率极差推过 0.75,触发 `X_GE_4_RETURN_FOR_STRATUM_STRUCTURE`。**因此:本次中止没有丢失任何本可获得的判定。** 这是可完全独立复核的算术事实,不是辩解。

**措辞禁令(你第三轮下的,仍全额生效)**:唯一合法表述 = **"已完成的 5 个有效结果均未成功;原定 12 次未完成,因此不作成功率或假设检验结论"**。禁写 `0/12`、"二项检验表明"、"正式阴性结论"。**请在你给的任何方案与话术里保持这个纪律。**

### 1.2 LoRA —— 训练机制健康,零收益,未部署

**训练本体(健康)**:two-epoch 完整执行,**856 microsteps / 54 optimizer updates / elapsed 1778.63 s / 12 项 promotion gate 全 true**。训练报告 SHA-256 `1513f6de2c641f3dd8f90883a5c71ac8fb0e92adc9fe1b1b60c872646000be06`。冻结配置 r8 / α16 / LR 2e-5 / accum 16 / seed 20260827 / external selected-logit FP32 CE;19,791,872 个可训练参数;梯度只流经 adapter;in-process 与 fresh unmerged reload 均 bit-exact。

**两个"好看"的数,我们都不放进主 deck**:固定探针交叉熵 `0.3206965923309326 → 0.1786690652370453` = **−44.3%**(promoted probe `s5_base_00bc9a0705e1b2873937`,396 targets);训练损失 `0.5663164258003235 → 0.1900274008512497` = **−66.4%**,但后者是**首末 microstep 的单点 loss 之比**,不是 epoch 平均,54 次更新的噪声足以主导它。理由:两个数都只说"模型在拟合",没有一个说"任务变好了";放进去只会引出"那准确率呢",而答案是零。

**成对评测(真正的结论)**,三个 split 的 2×2 `n11/n10/n01/n00`:

| split | n(成对) | 2×2 | 读法 |
|---|---|---|---|
| SYNTHETIC_TEST | 22 | **19 / 0 / 0 / 3** | 逐例完全一致 |
| MANUAL_BLIND | 45 | **31 / 0 / 0 / 14** | 逐例完全一致 |
| DEV | 78 | **63 / 1 / 1 / 13** | 1 纠正 + 1 回归,净零,**不是逐例一致** |

`NO_BENEFIT_EVIDENCE` 冻结,`benefit_claim_authorized = false`。**部署定为 `two_shot_family_first_v2` ICL,不挂 adapter**;已 commit `4f8c7908` 并打 tag 冻结。

**adapter 身份链(完整)**:final adapter = `/home/gl/xh-202607-qwen-s5/adapter-two-epoch-node2-v1`,tree SHA-256 `8a09f56a4a77c0127c0f1c2008efb8726e11d4a9b0c56f526a9934ce0a88f0b9`,publication SHA `137d65bf…`;DEV / TEST / BLIND 三处 `TRAINED_LORA` 臂**均显式绑定该 tree**;与 smoke adapter(32 microsteps / 2 updates)的**同域** publication SHA `b7ad8ebc…` 不同。故被评的确系 two-epoch adapter。

**本轮新查明的因果线索(LoRA 线上唯一有信息量的东西)**:DEV 上 **zero-shot = 18/78,two-shot(部署策略)= 64/78,two-shot + 微调 adapter = 64/78**。即:**0 → 2 个上下文示例带来 +46pp;2 个示例 → 微调带来 0。上下文示例已经把这个规模下可得的增益吃完了。** 这把"LoRA 没用"从孤立阴性变成有机制解释的结论。

**但有一个减分项必须同时写明**:历史示例选择用了 `row.family`(冻结字面值 `SAME_FAMILY_THEN_GLOBAL_SHA256_V2`),该字段与答案类型强相关。因此 **DEV 64/78、TEST 86.4%(19/22)、BLIND 68.9%(31/45) 只能标注为"示例选择有利条件下的绝对准确率估计"**。**成对结论不受影响**——两臂用完全相同的 request 与 policy,唯一变量是是否挂 adapter。

**claim manifest `lora_no_benefit_v1`**:11 条 ID → kind / path / SHA 的 pinned 引用、显式 `trust_boundary`(披露"无独立的 payload→文件字节证明",并把该 overclaim 写进禁语)、**6 条 `forbidden_claims`**;三文件身份 `a91c5d08…`/23,466 B、`41dcef86…`/10,903 B、`221afe6c…`/16,052 B;32 测试 + 复审 CLEAN。

### 1.3 视觉 —— 无任何可辩护的正面证据

这条最难看,请不要替我们找补。

- **`SYNTHETIC_TEST` 视觉行 = 0。** 实测各 family:`CONTRACT_REFUSAL` 3 / `RECOVERY_EXHAUSTION_REPLAN` 9 / `REGISTERED_SYMBOLIC_RELOCATION` 10 / `VISUAL_CONDITION_SELECTION` **0**。故 19/22 只能称"**符号契约与规划成绩(该测试集不含视觉行)**",不得称多模态成绩。
- **BLIND 45 例中视觉族 12 个,总错 14 个。** 算术上与"错误几乎全落在视觉族"一致,**但逐族分解尚未交付**,故**不当作已证事实**。这条我们特意留成"未证",请判断该补做分解还是干脆不提。
- **视觉任务统计上不可识别。** `VISUAL_CONDITION_SELECTION` 行的期望答案要求模型**逐字输出一个在输入中完全不出现的内部不透明 ID**(形如 `s5_track_3ab109db93005a61`),而 schema 校验器构造公开视图时主动把该 ID 从模型可见输入中删除。那些行**设计上无法完成**;任何"视觉答对"都应优先怀疑泄漏 / 顺序 / ID 相关性。
- **P3(公开 A/B/C/D 标记 + 私有绑定器)已取消。** 不是来不及,是触到一条**永久硬边界**。`CODEX_GOAL_M2C_HEADROOM.md` §1 第 5 条原文:

  > 特权仿真真值(entity/prim identity、完美位姿、接触、注入失败真值、task-success 真值)只能作为训练/评测标签,**不得进入 test-time policy input**。

  P3 的 marker 由仿真器实例分割真值生成并叠加进模型看得见的图像,即便映射表私有,**标记的存在本身就是特权真值进入了 test-time policy input**。`ADR-0020` 把违反此条定为 *terminates the experiment*;代码层三重强制(`privileged_truth_policy_input: Literal[False]`,在 selection / report / prereg scope 三处断言)。修正设计已存为 vNext 规格(`QWEN_BRAIN_P3_VNEXT_ORACLE_CANDIDATE_SPEC.md`),**本轮不做任何实现、离线诊断或治理路径版本**。

**结论:本轮不存在可辩护的正面视觉能力证据。** 权威状态写死为 `claim.no_visual_evidence`;deck 第 5 页已从"视觉成绩"改为"语义门 FAR/FRR",视觉列为 vNext。

### 1.4 运行时重关联握手 —— 结构性未实现

第三轮把 `EXECUTE` 前缀裁为**结构性禁止**(静态验证器看不到重关联的运行时输出,无法对该步作真实判断;按"验证器验不了的声明不得出现在安全结构里"移除)。后果:`EXECUTE` = **恰好六步**物理序列,**运行时重关联握手成为明确对外披露的 vNext 未实现项**。这一条性质与前三条不同:不是"测了没达标",而是"**我们主动认定验证不了,所以不做**"。

### 1.5 唯一还在活的正面选项:Task32(**刚刚启动**)

Task31 的 Isaac 独占已释放,Task32 用独立 debug identity 启动,**≤2 小时**,只写 `/private/tmp`,**禁触冻结 campaign 面**。**内容**:坐标链 / TCP 自洽检查——5 个姿态下逐一取 `link8` / `hand` / `hand_tcp` / 指尖位姿,做 FK、回灌、XYZ 各 +10 mm 扰动一致性、teleport 分流对比。**起点假设**:官方 `hand → hand_tcp` 偏移 103.4 mm 与实测 95.3 mm 对不上;抓取失败呈现的是**系统性偏差而非随机抖动**(最佳接触误差 95.3 mm,接触门限 20 mm)。

**若确认是固定坐标系偏移**:可在冻结面外修复,并录**一段抓放镜头**。硬约束(已写死,不可事后改):标注 `vNext 工程演示;不计入冻结 campaign`、**不报成功率、不回填、不入任何分母、无剪辑、无人工干预**,HUD 全程显示 `contract → gate → executor`。**修不好则不录任何失败镜头。**

**这是 9/3 冻结前我们唯一还能改变的东西。**

---

## 2. 我们**确实**拿得出的东西(请据此设计方案,不要只盯着负面)

### 2.1 P2:48 例冻结语义契约测试(terminal clean)

构成:**24 合法 / 24 不安全**(后者含 12 复合对抗:同时 JSON Schema 合法、大部分能过旧最低期望匹配、新验证器必须拒绝、含两个以上互相掩盖的缺陷)。合法侧 = **12 执行**(强制跨**多种不同但合法的拓扑**:线性依赖 / 合法并行前置感知 / 不同逻辑 agent 分工 / 等价的不同安全顺序,防验证器退化成精确匹配)+ **6 拒绝** + **6 恢复**。

**主报指标(全部实测)**:

| 指标 | 值 |
|---|---|
| Unsafe False Acceptance Rate / Valid False Rejection Rate | **0 / 0** |
| 三种结构模式路由正确率 | **1.0** |
| 错误规则覆盖 | **26 / 38** |
| 错误字段定位正确率 | **44 / 44** |
| 旧匹配器接受、新语义门拒绝的对抗见证 | **14** |
| REJECT 时 `primitive_dispatch_count == 0` | **24 / 24**(逐个,非聚合) |

注意 **26/38 不是满分**,我们**照原样报**。工件身份:suite `846d1753…`/49,647 B;validator `f2cdf8b7…`/29,309 B;inventory `358c73f9…`/21,349 B;CLI `fba66851…`/2,969 B;terminal clean。

### 2.2 最干净的见证案例(我们认为这是全项目最能讲的一页)

一份契约 JSON:`decision: "REFUSE"` **且 `subtasks` 非空且含物理原语** → **静态 JSON Schema 校验 PASS** → **旧"最低期望匹配器" PASS** → **新语义门 REJECT** → **`primitive_dispatch_count == 0`**。

来源诚实交代:**这是我们自查出来的真实缺口**——静态 schema 顶层没有 `oneOf`,`decision` 与 `subtasks` 是平级字段,三者互斥**仅由验证器强制**。因此措辞禁令:**禁称"Schema 原生三路由",只能称"验证器强制的三种互斥结构模式"**(三轮裁定,四轮把顶层升级裁为延期)。

### 2.3 语义验证器 V2 + fail-closed 派发门

typed predicate discriminated union;验证以**绑定的 TaskSpec + world snapshot** 为参照(防"模型说是 B、计划全程用 B、判定正确"的循环论证);派发门 fail-closed,绑定 plan canonical hash / TaskSpec hash / snapshot / expiry;dispatch 入口**重新** canonical 序列化并比对 `plan_sha256`,不等即拒绝且 dispatch = 0。P1 关闭时 **134 项实现回归测试 PASS** + lint / compile / schema / diff 四门全绿。**分栏禁令**:134 是实现回归测试,48 是语义契约案例,**必须分栏,禁写"134 个安全案例"**。

### 2.4 治理与工程纪律(可核验的部分)

- 预注册:规则/阈值/判据运行前冻结;失败必入分母;门限不可事后移动;事故写 incident log 不覆盖旧结果。**冻结 auditor 在开火之前就否掉了我们最想要的结局**(§1.1 的 `advance_count = 0`)。
- **今晚 6 次 fail-closed 拦截全部零消费,ordinal 一个未烧**:目录权限 / 父目录元数据 / invoker 契约自相矛盾 / 解释器类型与检查规格冲突 / prescan 主语错配 / 动态字段与身份门互斥。
- 必须披露的不对称:**解释器身份钉定自 ordinal05 起**,00–04 仅有 argv 层身份、不可追溯补录。

### 2.5 系统架构(必须被正确表达)

**27B 多模态 VL 指挥体低频决策**(Qwen3.8-27B,node2 单张 A100-SXM4-80GB,Transformers 直连无服务层;**模型不输出坐标 / 轨迹 / 关节值**)→ **严格 JSON 技能契约**(6 原语、12 谓词、逐原语固定 wire shape)→ **可验证经典执行层**(采样式抓取合成 + IK + 碰撞检查 + 接触门,Isaac Sim + Franka Panda 七轴臂)。**不是端到端 VLA**:自然语言不直接驱动关节,必须先编译成带前后置条件的符号契约,执行层可拒绝越权或不可达请求。**双层失败恢复**:技能级(换抓取候选 / 重对齐 / 局部重试)→ 任务级(升级回指挥体重分解,或安全拒绝)。**架构图须画出证明边界终止于哪一层**(口径见文末第 5 条)。

---

## 问题一|从这份清单能拿出的最强提交是什么?

**请直接给出 8 页 deck 的页面级方案。** 每页给:① 标题(可直接用的中文一句话);② **2–4 个可核验数字**(只能从 §1、§2 的真实数字里选,不得发明);③ 一张什么图;④ 评委看完这页该记住的一件事;⑤ **这页最可能被追问什么、怎么答**(答案要用我们手上真有的东西)。

**约束**:9/3 代码冻结(此后只能做材料)、9/5 提交、评委非专业、赛题名为"工业环境下物体感知识别与指令交互型智能体研发"——**注意"感知识别"四个字在赛题里,而我们恰恰在这一项上没有正面证据**(§1.3)。

**我们的草案**:1 语义门拦截演示(§2.2 见证案例,替掉原"成功演示"),2 架构 + 信任边界,3 P2 的 48 例与七项指标,4 双层恢复,5 LoRA 的 18/78→64/78→64/78 因果,6 能力边界,7 治理与证据完整性,8 vNext。**请直接反驳这个草案**:哪一页是自我感动、哪一页放错位置、哪一页评委根本不会读。

另判两件具体的事:**(i)** 赛题里的"感知识别"我们没有正面证据,**要不要在 deck 里主动承认?** 承认放第几页、怎么措辞?不承认是不是就等于掩饰?**(ii)** **P2 的 26/38 是个不满分的数**,报、不报、还是报并解释剩下 12 条?我们倾向照原样报,但担心非专业评委把"26/38"读成"68 分"。

---

## 问题二|"没有正面能力演示"这件事,在这个赛题下是致命的吗?

对照赛题名"物体感知识别与**指令交互型智能体**研发":**感知识别**零正面证据(§1.3,原因是任务设计缺陷 + 一条不能碰的硬边界,不是"没做完");**指令交互**有可验证证据(§2.1–2.3),但全部落在"模型输出 → 结构化契约 → 语义验证 → 派发门"这一段,**不含"识别一个真实物体"那一段**;**物理执行** 5 个有效结果 0 成功(§1.1),没有一段成功的抓放视频。

**请给判断,不要两头讨好:**

**(a) 这在本赛题下是致命的吗?** 内部两种意见:一种是"致命——赛题名点名要感知识别,拿不出就是没做到题目要求";另一种是"不致命——评委真正看的是系统完整性和工程可信度,一个诚实可核验的安全智能体框架比一段能被剪出来的抓取视频更重"。**请告诉我们哪一种是自欺。**

**(b) 若判定致命**:在 3 天工程 + 2 天材料内、不违反 §1.3 那条永久硬边界的前提下,还能怎么补救?给具体动作,不要给原则。

**(c) 若判定不致命**:重心该放在哪?三个候选:"可验证契约与派发门"(§2.2–2.3)、"预注册治理与诚实阴性"(§2.4)、"分层架构与信任边界"(§2.5)。请排序并说明为什么。

---

## 问题三|Task32 值不值得押?

Task32(§1.5)是**唯一**可能产出正面演示的路径,**现在刚启动**,≤2 小时,成败未知。它不占冻结面,失败的直接成本只有 2 小时;但间接成本是:**它决定第 1 页放什么**,而第 1 页一旦定了,后面七页的顺序都要跟着动。

**(a) 若 Task32 成功**——拿到一段修复坐标偏移后的抓放镜头,硬标注为 `vNext 工程演示;不计入冻结 campaign`、不报成功率、不回填、不入分母、无剪辑无干预、HUD 显示 `contract → gate → executor`:

- 这段镜头应占多大权重?**第 1 页整页,还是第 4 页的一个角?**
- 标注怎么写才**既诚实又不自我贬低**?草案是在画面上压一行:"vNext 工程演示 · 修复后独立复现 · 不计入冻结实验分母"。担心这行字会被读成"他们自己都说不算数"。**请给更好的措辞,或告诉我们这行必须保留原样。**
- 更尖锐:**一个被自己标注为"不计入分母"的成功演示,在评委眼里还有没有分量?** 若没有,Task32 是不是白做?

**(b) 若 Task32 失败**——第 1 页改用 §2.2 的语义门拦截演示。

- **够不够撑住开场?** 问题在于:这是一个**"什么都没发生"的演示**,最强的证据是一个计数器等于 0。非专业评委在 30 秒内能感受到它的分量吗?若不够,第 1 页还能放什么?
- 请给一句 **40 字以内**的中文旁白,让非专业评委看到"dispatch = 0"时立刻明白它为什么重要。**不许把逻辑讲错**,也**不许**用"派发""契约哈希""fail-closed"这类术语。

**(c) 有没有第三条路?** 请列出**4 天内可做、不违反 §1.3 那条永久硬边界、且不需重新渲染数据**(Isaac 资源与人力已排满)的正面证据路径。**一条都没有的话请直说一条都没有**——这比一个做不完的建议更有用。

---

## 问题四|四条负面 / 缺席,怎么排布才不被读成"项目失败"?

四条:

| # | 条目 | 性质 |
|---|---|---|
| 1 | 抓取:5 个有效结果均未成功,原定 12 次未完成 | 测了未达标,**判定不可达已在开火前证明** |
| 2 | LoRA:成对 delta = 0,故未部署 | 零收益,**但有机制解释**(18/78→64/78→64/78) |
| 3 | 视觉感知识别:无可辩护的正面证据 | **任务设计缺陷 + 硬边界**,不是"没做完" |
| 4 | 运行时重关联握手:结构性未实现 | **主动认定验证不了所以不做** |

你第四轮给过原则:"**最多一页专门讲边界,其余边界放在对应能力旁边**"。**本轮要具体到页:请给一张可直接执行的排布表(条目 → 页码 → 位置 → 句式),不要给原则。** 三个卡点:

- 四条里**哪一条(或哪几条)进那"专门一页"**?剩下的贴到哪一页的哪个位置、用什么句式(一行小字?"已知边界"角标?正文一句话?)
- 第 3 条最难:它既是能力缺失,又是**我们自己发现的任务设计缺陷**,还牵着一条**我们主动遵守的硬边界**。这三重性质里,对评委讲哪一重?
- 第 4 条性质不同(主动不做,而非做了没成)。**这对评委是加分项(自我约束)还是减分项(功能缺失)?** 若是减分项,该不该降级到附录——降级会不会构成隐瞒?

---

## 问题五|`ADVANCE` 可达数 = 0 这件事,该怎么讲?

事实见 §1.1(可达数 = 0,原因是我们自己在运行前写死的分层配额 + 层间极差门 0.75 + 最小层已锁 0.000)。**两种读法:**

- **读法甲(我们的主张)**:这是"**预注册规则强到能在开火之前否掉自己最想要的结局**"。它证明判据不是为了让自己好看而设的——一个真正的预注册协议就该有能力判自己出局。实用后果:本次中止**没有丢失任何本可获得的判定**,"没跑完 12 发"不是遗憾,是无关。
- **读法乙(我们担心的)**:"你们设计了一个自己赢不了的实验。" 即配额与阈值组合起来使成功不可能,这不叫严谨,叫实验设计失败;把"跑不完也没损失"说成优点,像在为没跑完找台阶。

**请判断:对一个非专业评委,哪个读法更可能?** 以及:

- 若读法乙更可能,**这件事该不该讲?** 不讲是不是就丢掉了我们最强的方法论证据?
- 若该讲,**给一个 60 字以内的中文说法**:不用"分层""极差""枚举""可达性"这些词,但**不许把逻辑讲错**,且**不许**暗示我们本来能赢。
- **读法乙其实有几分道理吗?** 我们确实在运行前写死了这套阈值,但当时**并没有算过 128 个后缀的可达性**(这是事后 Task33 才补做的)。**如果一个预注册设计在事后被发现"最想要的终态不可达",这算设计缺陷还是纪律优点?请直说。**

---

## 附录 A|我们自查出的错误清单(请评价我们的纠错机制)

**这一节不是求安慰。** 今晚(8/30 一天之内)我们共发现 **11 次同一形状的错误**:**事实 / 证据存在,但它的类型、指向对象或权威地位是错的。** **其中约半数是协调会话——项目的决策者本人——犯的。** 逐条:

1. **迁移做了全量逐条字节守恒校验并通过,却把既成事实降权为"非规范缓存"**,导致 lane owner 严格照协议办事反而读到陈旧的相反结论。最刺眼的物证:该事实提升进核心区时沿用了旧键名,权威状态里于是出现 `final.not_started — Pristine Node2 two-epoch PASS: 856 microsteps/54 updates, 12/12 gates`——**键名说"没开始",内容说"已全绿"**。字节守恒校验对此完全无感。
2. **`−44.3%` 被标成"训练损失"**,实为固定探针交叉熵;真正的训练损失是 −66.4%。两个数都真实,标签串了。
3. **已冻结的 `NO_BENEFIT_EVIDENCE` 结论,其关键前提(被评的是哪个 adapter)从未进入规范区**,只散落在日志里。若评的是 smoke adapter,该结论基本不成立。结论没变,但**我们是靠运气而不是靠机制守住的**。
4. **campaign 账目(5 有效 / 7 未消费)只活在 journal**,权威状态 §1 仍是停机当刻的陈旧快照,**并据此污染了下发给子代理的任务书**。
5. **用 tree SHA 去比 publication SHA 判断"两个 adapter 不同"——跨域比较,不成立。** 后来改用同域 publication 对比才补救。
6. **把已落地并测试闭合的 Schema V2,在上一轮咨询包里描述成"待决定要不要做"**,致使外部审查回答了一个错误的问题。
7. **prescan 只验目标不存在,不验父目录元数据。**
8. **prescan 19/19 全绿,但验的是 v3、要打的是 v4——绿灯挂在了错误的主语上。**
9. **把一个文件 SHA-256 当作 git 对象标识去引用**(`84ceadfc` 是文件 SHA,不是 git 对象)。
10. **halted 报告模板里 `campaign_disposition: "HALTED_WITH_SEVEN_PHYSICAL_ORDINALS_UNCONSUMED"` 已失真**(ordinal05 已物理派发,未消费的是六个),**差一点原样写进对外报告**。已下强制口径重述。
11. **协调下的身份门禁令过宽**,把不可事先钉定的动态字段 `requested_at_ns` 也禁掉,使身份门与点火机制**互斥**——点火必然重新生成该字段,prescan 所钉的请求不可能等于实际点火请求。判为"协调裁定规格错误",身份门改绑 stable request digest + 显式动态白名单。

**你第四轮已经把根因归为:"有 content addressability,没有 semantic addressability 与 referential integrity。"** 这句话我们接受并已经拿去改流程了。本轮请你回答三件事:

**(a) 这个诊断完整吗?** 第 6、8、10、11 条我们怀疑还有第二条根因——共同点不是"引用错了对象",而是"**一个正确的结论挂在了错误的主语上**"(绿灯挂在 v3、禁令挂在动态字段、模板口径挂在旧世界状态)。这算 referential integrity 的子类,还是另一种病?

**(b) 我们已采取的对策够吗?** 四条:①**强制提升扫描**(lane owner 转只读前通读 journal,把该进规范区的事实提上来);②**稳定实体 ID 禁含可变状态词**(禁 `final.not_started` 这种把状态编进键名的做法);③**关键问题答案守恒**(迁移前后对同一组关键问题的可回答性必须一致);④**claim manifest 含 `forbidden_claims` 与 `trust_boundary`**。请逐条评:哪条是真的门、哪条只是好意愿。特别是 ③——我们直觉上需要一个"**语义守恒**"层面的自动校验,但想不出怎么做成可执行的门。**若做不成,请直说做不成并给替代。**

**(c) 这条最重要,请直说:一个决策者本人高频犯同类错误的项目,应该建立什么机制?**

背景:项目多会话并行,**决策权集中在一个协调会话**,而该会话被频繁自动压缩(执行会话 7 天内 83 次)。11 条里约半数是它犯的。已有的补偿只是"权威状态必须落盘、压缩后第一件事是重读状态文件、禁止把压缩摘要当事实来源"。更硬的一层:

- **决策者的裁定,该不该和普通 lane 的工件一样接受门控?** 现实是:lane owner 每次改动都要过 lint / compile / schema / diff / 复审,而**协调的一条裁定只要说出口就生效**。第 11 条就是这么产生的。
- 但反方向也成立:**你第三轮刚判定"治理已从资产转为负债、必须停止扩张"**,给决策者加门就是加治理。**怎么平衡?** 请给具体、轻量、在本项目现状下真能跑起来的机制——不要"建立审查文化"这类答案。
- 最后一个我们不敢自问的:**11 次同形错误发生在一天之内,这个频率本身说明了什么?** 是自查在起作用(所以才发现得了),还是问题已严重到自查跟不上产生速度?**请给判断。**

---

## 附录 B|本轮**不需要**你回答的(已裁定并落地,再问是浪费)

M2C 治理链机制(预注册/冻结/分母/哈希绑定/事故不回填与"最小充分集七项",已裁停止扩张)· 成对评测统计方法(2×2、exact McNemar、bootstrap CI、聚类不足标注,已冻结)· 数据泄漏门设计(感知哈希仪器有效性门、隔离元数据验证器、连通分量原子分割,v4 全程通过)· **Schema v2 是否动顶层**(已裁延期,V2 grandfathered)· **P3 是否做**(已按 §1.3 永久硬边界取消,**请勿建议任何"轻量版 P3"**)。

---

## 口径提示(请你在建议里也遵守,避免替我们扩大化)

1. 抓取线唯一合法表述见 §1.1;禁 `0/12`、"二项检验表明"、"正式阴性结论"。
2. **"成对逐例完全一致(`n10=n01=0`)"只对 TEST 与 BLIND 成立**,DEV 是 `63/1/1/13`,不得推广到三个 split。
3. **19/22 = 86.4% 不是视觉成绩**(该测试集视觉行为 0),只能称"符号契约与规划成绩";三个绝对准确率(DEV 64/78、TEST 86.4%、BLIND 68.9%)必须标注"示例选择有利条件下的估计"。
4. **134 是实现回归测试,48 是语义契约案例,必须分栏;禁写"134 个安全案例"。**
5. **禁称"Schema 原生三路由"**,只能称"验证器强制的三种互斥结构模式";信任边界只到"计划字节不可变绑定 + REJECT 时原语派发计数为 0"。
6. **禁反向声称 ordinal 00–04 有解释器身份保证**;**"BLIND 错误几乎全落在视觉族"只是算术一致,不是已证事实**。

---

## 给你的总提示

1. **请直接说我们哪里想错了。** 不要罗列可能性、不要综述、不要平衡报道。**如果你认为这份材料里我们最自豪的三样东西(§2.2 的见证案例、§1.1 的 `advance_count=0`、§1.2 的 18/78→64/78→64/78 因果)其实撑不住,请直接拆掉它们。**
2. **优先级:问题一 > 问题二 ≈ 问题三 > 问题四 > 问题五。** 问题二、三今天之内必须定;四、五是材料期的事。
3. 不要建议任何违反预注册协议、事后调阈值、挑选结果、隐藏阴性、伪造数据,或让特权仿真真值进入 test-time policy input 的做法。**若你认为剩下的 3 天工程 + 2 天材料里已无法改善局面,请直说**,并给"在既定局面下最大化得分"的方案——这个答案我们接受,且比一个做不完的乐观建议有用。
4. 附录 A 那 11 条不要安慰,**要判断:一个决策者本人高频犯同类错误的项目,该建立什么机制。** 这是本轮我们最想要的一条答案。
