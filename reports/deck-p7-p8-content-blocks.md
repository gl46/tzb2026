# Deck 内容块｜第 7、8 页
维护人:tzb-fe　2026-08-31　**口径以 `CLAIMS-SHEET-20260831.md` 为准,冲突以其为准**
**单一来源纪律**:本文件不复述其他页已有的数字;需要引用时写指针。

---

## 第 7 页｜唯一一页能力边界

### 标题
> **本次提交只主张已经验证的部分**

### 版面原则(重要)
这是**全 deck 唯一一页专门讲边界**的页。其余边界必须贴在对应能力旁边(视觉→第 2 页灰标;运行时握手→第 5 页虚线)。
**页面主体不得写成"我们坚持不让仿真器真值进入策略,因此很严谨"**——那听起来像用原则为能力缺失找理由。
**正确顺序**:① 先承认没有有效视觉证据 → ② 再说明原任务不可识别 → ③ 最后一句才说明没有用特权提示去伪造一个分数。

### 主体:能力覆盖矩阵

| 链路 | 当前状态 |
|---|---|
| 指令 / TaskSpec → 符号契约 | 有限证据 |
| 执行前验证链(契约层 + 语义验证器) | **已在冻结 synthetic harness 中验证** |
| 视觉物体识别 | **无可辩护正面证据** |
| 稳定抓放闭环 | **未建立** |
| 运行时重关联握手 | **未实现** |

### 两张主卡片

**左卡｜抓取**
数字与冻结原话**只由** `reports/deck-m2c-campaign-block-20260831.md` 提供,本页引用不复述。

**右卡｜视觉**
> **当前视觉任务要求输出输入中不存在的内部 ID,不能支持视觉识别能力主张;相关结果不纳入本次能力成绩。**

### 三个可上页的数字
- Synthetic Test 中视觉样本:`0`
- 视觉族结果:`0/12`(**必须紧跟**:对任何**仅访问当前公开输入、且无外部 ID 映射或泄漏**的方法,该不透明 ID 不可恢复;这是任务缺陷的证据,**不是模型视觉能力的结论**。**禁用全称"任何方法都不可解"**)
- 尚未执行的 ordinal:`6`

### 评委应记住
> 项目完成了可验证的指令执行安全层,但没有完成整条感知—抓取闭环。

### 最可能追问｜"这是不是意味着你们没有完成题目?"
**标准答法**
> 是**部分完成**,不是完整完成。我们已经完成并量化了指令契约和执行安全层;视觉识别、稳定抓取和在线重关联没有达到可以对外主张的证据标准。

**禁止答法**:"但我们的治理更有价值。"——回避题目,且违反 §0 的贡献主次裁定。

### 禁用表述
- 把特权真值边界讲成主要成就
- 用治理术语替代对能力缺失的直接承认
- 把 `0/12` 解释为"模型视觉能力差"

---

## 第 8 页｜冻结交付与下一步

### 标题
> **一套可复核的执行安全层已经冻结,未完成的链路也被明确固定**

### 左栏｜交付物栈
```
语义验证器 V2(typed predicate union · TaskSpec/world-snapshot 绑定)
执行安全门(plan hash · TaskSpec hash · snapshot · expiry)
48 例冻结语义案例套件
语义规则库存与覆盖分类
命令行工具与报告
```

### 右栏｜三项 vNext(只列三项,不铺开)
```
非特权视觉评测
运行时重关联握手
修复后全新物理 campaign(B1-FRESH)
```
详见 `reports/VNEXT-BRAIN-ROADMAP-20260831.md`。

### 可上页的数字
- **在冻结语义契约路径的七文件回归子集上**,`135` 项全部通过;**并另行**通过 lint / compile / schema / diff 各门(**这四项不在 135 之内**)
  **禁止裸写"135 项回归测试通过"**——那会被读成"回归套件通过"。`135` 是**七文件 scoped 子集**,不是仓库全量。七个文件的 selector 与 argv 已从产生它的 transcript 取证恢复;**tzb-fe 于 2026-09-01 按恢复的 argv 独立实跑,观察到 `135 passed`、exit 0**。**只说这一次观测,不写"135 可复现"**——那是从一次运行推出的一般性能力主张,与我们刚作废的"永远无法复现"是同一个错误的反号。

  **全仓数字必须完整带以下限定,缺一不可(2026-09-01 更新):**
  | | |
  |---|---|
  | 现行可核值 | `61 failed / 1886 passed / 5 skipped`,2026-09-01 实测,有记录的 shell 命令 / 原始 stdout / nodeid / 退出码回执 |
  | 旧值 | `14 failed / 1933 passed / 5 skipped`,2026-08-31 **一次执行**,记录为 `TRANSCRIPT_ATTESTED`(命令文本、结果行与 14 个 failed nodeid 均在产生它的会话 transcript 中,**transcript 在包外**);**完整 stdout 未捕获**(命令经 `tail -25`),**退出码不可推断**(无 pipefail);我们未重现它 |
  | selector | 两次均为**全仓扣除一个被 ignore 的 collection-error 文件**,不是真正的"全仓" |
  | 失败归属 | 61/61 全在 `test_m2c_*`;**19 个失败文件中只抽查了 1 个**,**不得说那 47 个全由冻结门导致**,也不得说"都是无害的" |
  | 变更扫描 | `find -newermt` 是 **mtime 证据不是字节证据**,范围排除 `.git`/`.venv`/`artifacts`,**且扫描结果非空** |
  | qwen_brain | 2026-09-01 **观测到** 359 passed / 4 skipped / 零失败。**不得写"不受冻结门影响"——那是没测过的因果主张** |

  详见权威清单 §6 的 v5、v6 更正与 `evidence/fullrepo-test-20260901/`。
- `48` 个冻结语义案例
- 被拒计划均未到达末端原语 dispatcher 调用点:`24/24`(冻结 synthetic harness)
- 代码冻结:`9/3`

### 必须同屏出现的溯源行(第七轮外审 P0,不可省)

凡本页出现 P2 的 `48` / `24/24` 或回归数 `135`,**同屏必须带这一行**:

> **48 例执行结果与 135 项回归,均为 2026-08-31 按已核验冻结输入确定性重导出;非 2026-08-30 原始运行回执,亦非 P1 原始 134 运行。**

**理由**:MANIFEST 与证据索引里标了 `REDERIVED_FROM_FROZEN_INPUTS_2026-08-31` / `NOT_ORIGINAL_RUN`,但这些标签**没有出现在观众看得到的页面上**。外审的原话是:观众会把这些数字理解成原始冻结运行直接留下的结果工件。**标签正确但读者看不到,等同于没有标**——这是第 19 条"正确但找不到的标签 = 错标签"的同一形态。

### 证据索引
**主 deck 不放长串 SHA,也不写项数。**只放索引入口,指向 `reports/appendix-m2c-evidence-index-20260831.md`:**证据索引区分冻结期预期摘要与当前字节摘要,并逐项标注支持范围与不支持范围。**

### 评委应记住
> 交付物包含一套可检查、可复核的冻结测试与证据子系统,而不是一段剪出来的视频。

> **禁止**"可运行、可测试"——**本交付集合内没有实现代码**,"可运行"不能由这一集合验证。若另附代码交付入口,再议。

### 最可能追问｜"既然不是完整系统,为什么仍值得提交?"
**标准答法**
> 因为它针对一个明确且可复核的问题:**本架构要求开放模型产生的候选计划先经过执行前验证链;本次证据止于冻结受测路径。**我们同时把未完成部分和证明边界固定下来,没有用无效的视觉任务或未完成的实验去扩大主张。

> **禁止**"开放模型产生的计划**不能**直接进入工业执行器"——那是对生产全路径的架构保证,而我们的证据只到 synthetic harness 与受测 gate 路径。"本架构要求先过验证链"是设计陈述,"不能进入"是全路径事实主张,二者不可互换。

### 收束句(全 deck 最后一句,与视频镜头 6 同源)
> **开放模型只生成候选符号计划;契约层与语义验证器组成执行前验证链。在本次冻结测试范围内,已覆盖的不安全计划被拒,均未到达末端原语 dispatcher 调用点。视觉识别和稳定抓放尚未完成。**

### 禁用表述
- 放长串哈希充当可信度
- 把 vNext 列成承诺或路线图式的"即将实现"
- 在本页重复第 6 页的 LoRA 数字或第 7 页的边界表述
