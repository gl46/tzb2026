# M1B-alpha fine-tune comparison

No pretrained or fine-tuned open-vocabulary checkpoint has been downloaded or
executed. Consequently AP, target selection, pose, latency and before/after
comparison metrics are intentionally absent. The configured one-epoch,
frozen-backbone path is a resumable plan, not evidence of a training run.
