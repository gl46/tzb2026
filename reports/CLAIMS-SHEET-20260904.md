# CLAIMS-SHEET 2026-09-04(新版包;仅列相对 2026-09-03 版的变更,其余条款沿用 `CLAIMS-SHEET-20260903.md`)

> 规则不变:本文件与任何旧文档冲突时以本文件为准;任何改动一律出新版本,不插改旧版。裁定人:tzb-fe(协调会话),用户已于 2026-09-04 批 B1–B9 与编译器主路。

## 变更 1(§0d 命名裁定改写,取代 0903 第 77 行)
主标题:**面向工业机械臂的符号契约验证与可审计执行原型**;副题:指代表达定位 · RGB-D 符号接地 · 确定性/受验计划 · 执行前校验 · Isaac 抓放。
首页底部固定一句:"当前证据为固定场景单次工程演示,不代表成功率、跨场景泛化或认证级安全。"
验证器只叫"符号契约校验器 / 执行前校验",不叫"物理安全验证器";明写不覆盖 ISO 10218 意义的物理安全。(外审 B1/B2)

## 变更 2(§0d 子能力表"感知识别"一行的限定)
S2(指代表达定位)与 S3(RGB-D 符号接地)**允许**作为"链路阶段 + 单次工程演示证据"陈述,**不得**作为"感知识别能力已完成"的主张;0903 §5 关于 SYNTHETIC_TEST 符号 ID 任务不可识别的结论不变、不外推到 Isaac RGB-D 这条线。

## 变更 3(现场默认策略)
现场默认 `compiler_primary`:六步计划由确定性符号计划生成器从绑定的任务规格生成,经同一冻结校验器;模型贡献 = S0 指令解析 + S1 目标存在性判定;`commander_model`(模型写计划)保留为开关。允许的说法:"模型决定意图,契约决定动作"。禁止:"模型不会规划"(它带范例时通过校验 15/15,n=15 单次,battery-v2 a 臂)。

## 变更 4(三个 15/15 各自钉死证据类型,禁止裸写)
| 数字 | 谓词 | 证据 | 允许的写法 |
|---|---|---|---|
| 15/15 | 27B+域内范例 **通过冻结校验器** | battery-v2 a 臂,n=15 单次 | "27B 加范例:通过校验 15/15(n=15,单次)" |
| 15/15 | 确定性生成计划 **通过同一校验器**,S4 调用 0 次 | compiler-primary-v1 accounting | "确定性生成计划通过校验 15/15,不是模型能力测量" |
| 产出 15 / 通过 0 | 27B 无范例(9/3) | battery-v1 | 只能写"产出 15 份、通过 0 份",禁止写 0/15 |
21/24 仍是离线 k=0 符号套件,与现场帧数字不并排、不相互佐证。

## 变更 5(新增允许主张,均带分母与回执)
- 派发封条三类拒绝(同 nonce 重放、篡改、过期)在执行器运动前被拒(v4-switch-v1/condition-3;v17 常驻进程内重放仍拒)。
- 常驻执行器:收到请求到关节离开静止位 1.94 s 暖 / 6.97 s 冷(v17 验收轮 acceptance-receipt-v1;GPU1 直播轮 round-002-gpu1-round-6 为 2.90 / 7.89 s;两处引用都须带轮次身份),回执所记的控制指标与落点与 v15 参考逐位一致——指回执记录的具名指标,不是整次运行逐位复现(GPU 物理不可逐位复现,见各回执 known_limits);"控制路径逐字节相同"只指源码(58/60 函数与派发体逐字节同,v17-resident-v1)。
- RECOVER 段 3:扰动后前置条件重检判出绑定失效并发出 RECOVER(recover-v1/segment-3-receipt-v1;扰动请求 40 mm 实际横移 18.1 mm:根因已诊(segment-3-receipt-v1-amendment-v1)——夹爪未撤退、两指仍跨持,物体被推向指内侧被解算器顶回;±X 两向 17.86/18.31 mm 对称,排除单侧障碍;余约 8 mm 未完全解释,修正建议未施加。写法按此,不写"待查")。
- 未注册目的地显式拒绝并列出已注册项(v4-switch-v1/refuse-green-bin)。
- 直播:RTSP 60 fps/约 250 ms;直播不改变控制路径与上报指标。

## 变更 6(负结果,必须带且只能这样带)
S2 经派生的非冻结入口在 text 模式加载我方 vp-LoRA(冻结入口本身禁止 text+adapter)比基座差,不采用(14 格无一胜,零框 5→10/70);指挥官 LoRA 的 base 是 27B(不存在 4B 微调件),成对评测无增益未部署;VLA 离线 pilot 管线通、控制器映射未授权;QRM-lite 对规则基线无余量。写法见 `judge-package-v1/docs/negative-results.md`。禁止由此外推"微调无用"。

## 变更 7(措辞替换,全交付件生效)
"符号合同"→"符号契约";"计划编译器"→"符号计划生成器";"开放词汇定位"→"指代表达定位";"绑定"→"符号接地";不写"位姿估计"(只有中心与直径);"零派发"→"未到达末端原语下发调用点"(与 0903 §0d 逐字口径"末端原语 dispatcher 调用点"等价,首次出现可写"下发(dispatcher)调用点");"存在门"→"目标存在性判定";交付件不得出现"落盘/落地/易主/今晚/9/4 接手人"等内部用语。术语表:`judge-package-v1/docs/glossary-zh.md`。例外:此前裁定钉死并要求"原样引用"的措辞块(如 gen1282 回放措辞)保持原文,术语替换只管正文。

## 变更 8(记录修正)
estimator-bias-v1 的毫米数是对 FIRST_RETURNED_BOX 选框做的估计器评测;现场链路用注册颜色中位规则,在 fresh640 三帧上两者选中不同目标,故"现场规则+V11"在这三帧的偏差未测;deck/README 不得把该记录的数字说成现场规则的数字(修正件由 demo lane 出)。

## 变更 9(旗舰演示的配置必须随数字出现)
deck P7 / README 所引"一次工程演示走完从中文指令到六原语抓放"(run-v9/run-v11)的计划由**可选的托管 qwen3.8-flash(thinking on)**写出,链路为 v3(尚无 plan-policy 概念),派发请求为单独铸造;它是真实指令、真实执行、真过冻结校验器,但**不是**默认配置(27B + 符号计划生成器 + run_demo 一体铸封条)的成绩。凡引用该次,同页写明配置;默认配置下的合成一轮另有回执时才可写"默认配置下完成"。

## 变更 10(真值披露的唯一允许写法;取代 tzb-fe 16:3x 口授给 deck 的那句)
默认执行器(v15–v19 真值模式)逐字用 executor-port-v1-amendment-v2 的 corrected_wording_to_use_everywhere:"工程演示中,抓取与放置的状态闭环使用 Isaac 真值:倾斜门、举起判定、释放许可、落地判定,以及放置阶段的在手偏移(因而放置目标点)。真值不进 S0–S5 的任何一步,也不进接近段的伺服——接近伺服打的是感知中心。"
感知模式变体(v20)只能写:"抓取的成立不依赖物体位姿(真值仍逐步记录于 truth_for_evaluation_only,任何拿它做判定的消费方会直接 KeyError);本体感觉直径 29.747 mm(真值 29.760);落地判定改为释放后重观测,本次运行物体滚出料箱,执行器以 LANDING_NOT_OBSERVABLE 停止;变化检测器在真实深度上会触发,但因真值模式不撤手,它定位到的是仍悬在释放位的夹爪(与段 3 扰动只走 18 mm 同一根因:夹爪未撤退);感知抓取门比真值门粗,15° 内倾斜感知模式会放行。"禁止"抓取全程不读真值/不读位姿"的字面。
附加规则:凡引"接近误差(对感知中心)3.274 mm"必须同页并列"感知中心对真值 5.315 mm"(前者按定义两模式打平,只说明控制器跟目标,不说明感知有多准);"与 v15 逐位复现"只能写成"回执所记 4 项控制指标与落点逐位一致(整次运行非逐位复现)"。

## 变更 11(Isaac 域适配 text-LoRA 第 1 次:负结果条目,与变更 6 的 vp-LoRA 并列、不混写)
允许的写法:"从零训的 text 模式域适配 LoRA(基座冻结,206 帧/937 已验证实例(回执字段 verified_instances;真值锚定后经 V11 回绑验证)/400 步):held-out(同场景后段帧,非独立场景泛化)在有物格上总体更好——det@0.5 0.814→0.905、IoU 0.890→0.954;但按颜色崩——cyan det 0.688→0.063、green 1.0→0.0;14 格现场集零框 5/70→50/70、9 个 cyan 格全丢框、配对 45 次基座有框而 LoRA 无、反向 0;根因=类别失衡(cyan 48/green 36 正例 vs blue/magenta 720,cyan 柱是被抓对象多被夹爪遮挡),LoRA 对每色学成常量策略;不采用。"三条限制必随:cyan/green 已验证实例仅 16/10;held-out 为同场景时间切分;负例已压 25% 仍塌。
禁止:引用 14 格上 LoRA 的 IoU 0.9677 或 V11 0.898 mm(幸存者偏差:它只答了 20/70,V11 仅 5 格可算);**禁止引用 RESULT-v1 的"无物假阳 0.982→0.048"**——勘误(17:3x):那 332 个 no_box 格全是柱子在画面里被夹爪挡住的误标,真缺席 0 个;基座 98.2% 答框是找出了被挡的柱子(正确),LoRA 4.8% 是漏检 95%,与 cyan/green 塌陷同一现象;勘误件由 finetuned-live-v1 单出,RESULT-v2 并入;写"训了没用";写"泛化"。第 2 次(新静态帧均衡)结果另出变更。

## 变更 12(现场颜色规则:已观测到的错分路径,冻结面不动)
冻结现场选框规则 live-box-selection-rule-v1 的原型 green=120°/cyan=180°(分界 150°)。绿柱在冻结 dual100 场景原位成像 146.0–146.6°(10/10 判绿,距分界 3.4°);物体被挪过桌面的静态批上成像 136.9–160.7°,越过 150° 即判为 cyan——现场说"绿色圆柱"可能返回青柱(static-frames-v1-addendum-v1.json)。brown 30.0°/orange 30.1° 只差 0.1°,真橙色物体在两词间是抛硬币。两个量不并写:到分界的色相距离 3.3–3.4°;判决统计量 lead 余量 6.6°(到 cyan 33.3° 减到 green 26.7°),引用时点明定义。处置:不改冻结规则;README 与 live-demo.md 写"现场指令请用青/蓝/品红/红;其中只有青色跑过完整一轮,另三种没有整轮记录,列出是因为不在两处已知薄余量上"(蓝/品红/红的余量度数不写:原型表未由文档作者直接读取)并说明为何排除绿与棕/橙;禁止把四色都写成"已测试";deck 若提颜色规则须带此限。禁止写"支持任意颜色词"。
