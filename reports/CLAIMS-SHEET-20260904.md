# CLAIMS-SHEET 2026-09-04(新版包;仅列相对 2026-09-03 版的变更,其余条款沿用 `CLAIMS-SHEET-20260903.md`)

> 规则不变:本文件与任何旧文档冲突时以本文件为准;任何改动一律出新版本,不插改旧版。裁定人:tzb-fe(协调会话),用户已于 2026-09-04 批 B1–B9 与编译器主路。

## 变更 1(§0d 命名裁定改写,取代 0903 第 77 行)
主标题:**面向工业机械臂的符号契约验证与可审计执行原型**;副题:指代表达定位 · RGB-D 符号接地 · 确定性/受验计划 · 执行前校验 · Isaac 抓放。
首页底部固定一句:"当前证据为固定场景单次工程演示,不代表成功率、跨场景泛化或认证级安全。"
验证器只叫"符号契约校验器 / 执行前校验",不叫"物理安全验证器";明写不覆盖 ISO 10218 意义的物理安全。(外审 B1/B2)

## 变更 2(§0d 子能力表"感知识别"一行的限定)
S2(指代表达定位)与 S3(RGB-D 符号接地)**允许**作为"链路阶段 + 单次工程演示证据"陈述,**不得**作为"感知识别能力已完成"的主张;0903 §5 关于 SYNTHETIC_TEST 符号 ID 任务不可识别的结论不变、不外推到 Isaac RGB-D 这条线。

## 变更 3(现场默认策略)
现场默认 `compiler_primary`:六步计划由确定性符号计划生成器从绑定的任务规格生成,经同一冻结校验器;模型贡献 = S0 指令解析 + S1 目标存在性判定;`commander_model`(模型写计划)保留为开关。允许的说法:"模型决定意图,契约决定动作"。禁止:"模型不会规划"(它带范例时通过校验 15/15,n=15 单次,battery-v2 a 臂)。

## 变更 4(三个 15/15 各自钉死证据类型,禁止裸写)
| 数字 | 谓词 | 证据 | 允许的写法 |
|---|---|---|---|
| 15/15 | 27B+域内范例 **通过冻结校验器** | battery-v2 a 臂,n=15 单次 | "27B 加范例:通过校验 15/15(n=15,单次)" |
| 15/15 | 确定性生成计划 **通过同一校验器**,S4 调用 0 次 | compiler-primary-v1 accounting | "确定性生成计划通过校验 15/15,不是模型能力测量" |
| 产出 15 / 通过 0 | 27B 无范例(9/3) | battery-v1 | 只能写"产出 15 份、通过 0 份",禁止写 0/15 |
21/24 仍是离线 k=0 符号套件,与现场帧数字不并排、不相互佐证。

## 变更 5(新增允许主张,均带分母与回执)
- 派发封条三类拒绝(同 nonce 重放、篡改、过期)在执行器运动前被拒(v4-switch-v1/condition-3;v17 常驻进程内重放仍拒)。
- 常驻执行器:收到请求到关节离开静止位 1.94 s 暖 / 6.97 s 冷(v17 验收轮 acceptance-receipt-v1;GPU1 直播轮 round-002-gpu1-round-6 为 2.90 / 7.89 s;两处引用都须带轮次身份),回执所记的控制指标与落点与 v15 参考逐位一致——指回执记录的具名指标,不是整次运行逐位复现(GPU 物理不可逐位复现,见各回执 known_limits);"控制路径逐字节相同"只指源码(58/60 函数与派发体逐字节同,v17-resident-v1)。
- RECOVER 段 3:扰动后前置条件重检判出绑定失效并发出 RECOVER(recover-v1/segment-3-receipt-v1;扰动请求 40 mm 实际横移 18.1 mm:根因已诊(segment-3-receipt-v1-amendment-v1)——夹爪未撤退、两指仍跨持,物体被推向指内侧被解算器顶回;±X 两向 17.86/18.31 mm 对称,排除单侧障碍;余约 8 mm 未完全解释,修正建议未施加。写法按此,不写"待查")。
- 未注册目的地显式拒绝并列出已注册项(v4-switch-v1/refuse-green-bin)。
- 直播:RTSP 60 fps/约 250 ms;直播不改变控制路径与上报指标。

## 变更 6(负结果,必须带且只能这样带)
S2 经派生的非冻结入口在 text 模式加载我方 vp-LoRA(冻结入口本身禁止 text+adapter)比基座差,不采用(14 格无一胜,零框 5→10/70);指挥官 LoRA 的 base 是 27B(不存在 4B 微调件),成对评测无增益未部署;VLA 离线 pilot 管线通、控制器映射未授权;QRM-lite 对规则基线无余量。写法见 `judge-package-v1/docs/negative-results.md`。禁止由此外推"微调无用"。

## 变更 7(措辞替换,全交付件生效)
"符号合同"→"符号契约";"计划编译器"→"符号计划生成器";"开放词汇定位"→"指代表达定位";"绑定"→"符号接地";不写"位姿估计"(只有中心与直径);"零派发"→"未到达末端原语下发调用点"(与 0903 §0d 逐字口径"末端原语 dispatcher 调用点"等价,首次出现可写"下发(dispatcher)调用点");"存在门"→"目标存在性判定";交付件不得出现"落盘/落地/易主/今晚/9/4 接手人"等内部用语。术语表:`judge-package-v1/docs/glossary-zh.md`。例外:此前裁定钉死并要求"原样引用"的措辞块(如 gen1282 回放措辞)保持原文,术语替换只管正文。

## 变更 8(记录修正)
estimator-bias-v1 的毫米数是对 FIRST_RETURNED_BOX 选框做的估计器评测;现场链路用注册颜色中位规则,在 fresh640 三帧上两者选中不同目标,故"现场规则+V11"在这三帧的偏差未测;deck/README 不得把该记录的数字说成现场规则的数字(修正件由 demo lane 出)。

## 变更 9(旗舰演示的配置必须随数字出现)
deck P7 / README 所引"一次工程演示走完从中文指令到六原语抓放"(run-v9/run-v11)的计划由**可选的托管 qwen3.8-flash(thinking on)**写出,链路为 v3(尚无 plan-policy 概念),派发请求为单独铸造;它是真实指令、真实执行、真过冻结校验器,但**不是**默认配置(27B + 符号计划生成器 + run_demo 一体铸封条)的成绩。凡引用该次,同页写明配置;默认配置下的合成一轮另有回执时才可写"默认配置下完成"。

## 变更 10(真值披露的唯一允许写法;取代 tzb-fe 16:3x 口授给 deck 的那句)
默认执行器(v15–v19 真值模式)逐字用 executor-port-v1-amendment-v2 的 corrected_wording_to_use_everywhere:"工程演示中,抓取与放置的状态闭环使用 Isaac 真值:倾斜门、举起判定、释放许可、落地判定,以及放置阶段的在手偏移(因而放置目标点)。真值不进 S0–S5 的任何一步,也不进接近段的伺服——接近伺服打的是感知中心。"
感知模式变体(v20)只能写:"抓取的成立不依赖物体位姿(真值仍逐步记录于 truth_for_evaluation_only,任何拿它做判定的消费方会直接 KeyError);本体感觉直径 29.747 mm(真值 29.760);落地判定改为释放后重观测,本次运行物体滚出料箱,执行器以 LANDING_NOT_OBSERVABLE 停止;变化检测器在真实深度上会触发,但因真值模式不撤手,它定位到的是仍悬在释放位的夹爪(与段 3 扰动只走 18 mm 同一根因:夹爪未撤退);感知抓取门比真值门粗,15° 内倾斜感知模式会放行。"禁止"抓取全程不读真值/不读位姿"的字面。
附加规则:凡引"接近误差(对感知中心)3.274 mm"必须同页并列"感知中心对真值 5.315 mm"(前者按定义两模式打平,只说明控制器跟目标,不说明感知有多准);"与 v15 逐位复现"只能写成"回执所记 4 项控制指标与落点逐位一致(整次运行非逐位复现)"。

## 变更 11(Isaac 域适配 text-LoRA 第 1 次:负结果条目,与变更 6 的 vp-LoRA 并列、不混写)
允许的写法:"从零训的 text 模式域适配 LoRA(基座冻结,206 帧/937 已验证实例(回执字段 verified_instances;真值锚定后经 V11 回绑验证)/400 步):held-out(同场景后段帧,非独立场景泛化)在有物格上总体更好——det@0.5 0.814→0.905、IoU 0.890→0.954;但按颜色崩——cyan det 0.688→0.063、green 1.0→0.0;14 格现场集零框 5/70→50/70、9 个 cyan 格全丢框、配对 45 次基座有框而 LoRA 无、反向 0;根因=类别失衡(cyan 48/green 36 正例 vs blue/magenta 720,cyan 柱是被抓对象多被夹爪遮挡),LoRA 对每色学成常量策略;不采用。"三条限制必随:cyan/green 已验证实例仅 16/10;held-out 为同场景时间切分;负例已压 25% 仍塌。
禁止:引用 14 格上 LoRA 的 IoU 0.9677 或 V11 0.898 mm(幸存者偏差:它只答了 20/70,V11 仅 5 格可算);**禁止引用 RESULT-v1 的"无物假阳 0.982→0.048"**——勘误(17:3x):那 332 个 no_box 格全是柱子在画面里被夹爪挡住的误标,真缺席 0 个;基座 98.2% 答框是找出了被挡的柱子(正确),LoRA 4.8% 是漏检 95%,与 cyan/green 塌陷同一现象;勘误件由 finetuned-live-v1 单出,RESULT-v2 并入;写"训了没用";写"泛化"。第 2 次(新静态帧均衡)结果另出变更。

## 变更 12(现场颜色规则:真实边界,冻结面不动;取代 18:2x 前的写法)
实现 colour_box_selection_v1(REGISTERED_COLOUR_MEDIAN_BOX_SELECTION_V1):合格=饱和度≥0.15,选择=色相圆周距离最小者;**无最大距离阈值、无拒答**;色词不在注册表或无合格框时回退取第一框;localizer 返回 0 框才 NO_BOX_RETURNED。因此:①目标色框缺失/不合格时会选中别的颜色的唯一合格框,且 target_ref 仍按表达式命名;②"问绿拿回青"只在绿框缺失/不合格时成立(两框都在时绿柱 136.9–160.7° 对 green 的分 16.9–40.7 恒小于青柱的 60);③唯一挡板是 S1 存在性判定,下游不比对色相与色词。色相数字:原位 146.0–146.6°、静态均值 146.8°(距分界 3.2–3.4°;lead 6.4–6.8° = 2×距离,评委文档只写距离),挪动后 136.9–160.7°,最近色词归类 109/10/1(平局帧 000001 恰 150.000°,规则的平局处理未由本表声称);brown/orange 差 0.1°。评委路径:r4 之前该规则未随包启用(trace 记 FIRST_RETURNED_BOX),裁定 2026-09-04 18:4x 起进包并默认启用,以与 battery-v2/v4 账目配置一致;启用后须有一轮 trace 记该规则身份。措辞:README/live-demo 写"现场指令用青/蓝/品红/红;只有青色跑过整轮;绿因薄余量与未整轮不推荐";禁止"支持任意颜色词"、禁止把最近色词归类说成规则的选择结果。

## 变更 13(交付前最后一批:LoRA 第 2 次、推流默认、12 项终态、执行账目;22:3x)
- **Isaac 域适配 text-LoRA 第 2 次(加静态帧,同晚)**:唯一允许的写法——"held-out 真值锚定重判三条全过:检出 .845→.995、cyan V11 中心误差中位 3.243→3.194 mm(n=40/臂)、14 格现场集零框 5→0、held-out 紫色真缺席假阳 .991→0(同场景后段帧,非跨场景泛化);随包为**可选 profile**(`env/s2-lora-v1/`,派生入口+adapter+合并契约+SHA256SUMS),**默认仍是基座 text 模式**;默认切换的采用门(评委路径用 LoRA 侧车跑通合成一轮)**已于 23:26 跑通(lora-r8b,见增补)**,仍按裁定保持基座默认,理由=样本量(1 轮/1 帧/1 指令/1 候选框 vs 基座 6 次评委路径执行);§Verification 除 lora-r8b 行外无数字出自 adapter;`S2_PROFILE=lora` 为评委可开的开关。"(22:3x 原句"交付前未跑"作废,23:4x 更正)证据 `tzb-lanes/finetuned-live-path-v1/RESULT-v2-judgement-v1.md`(前缀 5b607a4b),重判未重跑推理、全取自既有回执。禁止:"已用于现场/直播";禁止把重判通过写成"已采用为默认";名字待用户给,格式仍为"名字(LocateAnything-3B + Isaac 域适配 LoRA)"。变更 11 的第 1 次负结果照旧列,不与第 2 次混写。
- **推流默认**:`scripts/start.sh chain` 的 RTSP 默认由 1 改 0(卡点 18:无 NVENC 的卡上 RTSP 注册失败→执行器起不来→EXECUTE=1 静默退成 dry-run)。写法:"推流是演示选项,不是执行前提;`RTSP=1` 显式打开"。
- **12 项终态(chxy 冷装)**:唯一写法——"18:32 全 12 项:1–6、11 PASS,7 FAIL(钩子回归),8/9/10/12 SKIP;19:06 定向重跑 7–12:7、11 PASS,其余 SKIP;按各项最新结果 8 PASS / 4 SKIP / 无 FAIL"。18:32/19:06 两轮都不是单轮无 FAIL(审查 R19 指出,22:4x 更正);**22:31 全 12 项(8/9/10 自足化后):11 PASS / 1 SKIP(12 无流)/ 0 FAIL**,可写"0 FAIL"仅指这一轮并引 `smoke-all-20260904T223111`。四份回执一并随包。
- **真执行账目(chxy)**:三轮、三枚 nonce(81a9932e…/8e098071…/2ba10c75…,取自 nonces 目录文件名),一枚一轮、账本只读、重启后不可重放;写法必须带"谁跑的"。
- **合成一轮 r4**:写法同 deck P7 三行边界;r4 的 A6 帧龄 4.30 s 只在轮次日志、不在回执,不作为回执数字引用;r4 的选框规则串是 FIRST_RETURNED_BOX_NO_RESPONSE_SELECTION,不兼作颜色规则演示。**dl-r7(22:25–22:28,规则默认启用后)DISPATCHED**:回执记 REGISTERED_COLOUR_MEDIAN_BOX_SELECTION_V1 + 规则文件 sha;bridge-receipt 记帧龄 4.35 s / 预算 30 s(可作工件数字引用,但须并写"其自述不是对世界状态的通过")、plan_read_from=compiled_plan;首动 4.38 s 只在端口输出、不在回执。
- **`EXECUTE=1 scripts/start.sh chain`(评委自己的启动路径,22:45 chxy 跑一次)**:允许的写法——"执行(非降级非 dry-run):exit 0、端到端 336 s(驱动日志计时)、DISPATCHED、6 条控制指令、COMPILED_PRIMARY、S5 PASS、注册颜色规则选框;帧龄 4.44 s / 预算 30 s;该路径不做世界再确认(world.status=UNOBSERVABLE,回执自述'不是通过')"。**必须并写**:跑通前提是手工给 `M2C_DATASET_ROOT` 与 `M2C_FRAME`(脚本默认帧名对不上取帧宿主的命名,open-questions 18)。首动 93.28 s 与 dl-r7 的 4.3774 s 都只在 outcome.json 的 executor.notes / 日志、不在 result-v17.json;dl-r7 时长写"采帧到执行结束 159.6 s",不写 163 s。禁止"已现场验证世界状态"。
- **路线 A(链在 Mac、执行器在 labserver)主轮未跑**:回执只写"已就绪、未跑、原因为让位评委路径",标签 LAB_ROUND_CHAIN_ON_MAC_NOT_THE_JUDGE_PATH。

## 变更 13 增补(23:3x:LoRA 侧车合成轮、卡点 23、退出码)
- **lora-r8b(23:26 chxy,demo lane)**:允许的写法——"用 LoRA 侧车在评委路径跑通一轮合成轮:DISPATCHED、6 条控制指令、COMPILED_PRIMARY、帧龄 5.09 s / 30;S2 由 adapter 服务的证据是三跳链(s2-session-receipt 的 ready_line.adapter.attached=true + entrypoint 摘要 fd833a52… + adapter receipt 摘要 9459f7b1… → receipt 内两文件摘要与包内副本一致),`s2_source_label=XH-Locator` 是合并契约的声明不是测量"。**必须并写**:该轮跑在 23:01 tarball 之后的工作树上,不是"某 tarball 的一次运行";只有 1 个候选框;默认侧车仍是基座(6 次评委路径执行用基座、1 次用 adapter),是否切默认待用户裁。禁止"LoRA 已成默认/已用于全部现场轮"。
- **合并契约**:`s2-contract-text-v4-lora-local.json`(PROPOSED),写法"以冻结 v4 为底,invoke/endpoint/upload/transport/query_policy/output/mode 逐字节相同,只改 entrypoint、新增 text-LoRA adapter 策略(与冻结 VP 策略并列)、标签、派生记录";lane 原 v4 契约因 ssh 传输不能在链镜像里起,只作历史件。
- **卡点 23(合法拒绝在 --execute 下 traceback)**:写法"已修:REFUSE TaskSpec 下 --execute 正常收尾写 outcome.json、reason_code=TASKSPEC_IS_REFUSE_NOT_EXECUTE、不铸请求不铸封、exit 0 并在 stderr 打 NOT DISPATCHED 一行;三条回归测试用真 trace;修复随 23:01 之后的新包交付"。退出码 0 是裁定(校验器认可的拒绝=演示正常工作),不得写成"拒绝会返回非零"。
- **第一次尝试(23:21)**:写法"派生入口作宿主进程起、契约 upload 路径是容器路径→每次请求 type=error→链正确拒绝(TARGET_NOT_LOCALIZED)";这是部署形态错,不是入口协议差(invoke/output 一字未改即跑通)。
- **`S2_PROFILE=lora EXECUTE=1 scripts/start.sh chain`(23:47 chxy,tzb-b9,从包内副本起)**:允许的写法——"LoRA 开关经评委启动脚本端到端跑通一次:执行、6 条控制指令、COMPILED_PRIMARY、S5 PASS、帧龄 4.59 s / 30、S2 由 adapter 服务(三跳链同 lora-r8b)";**必须并写**:首次尝试(23:40)起不来——23:01 包把 adapter 三件存成 0644 而回执要求 0400,冻结校验器比模式;已改树 + make_tarball MODE_CHECK;该路径不做世界再确认;默认仍是基座(基座 6 次评委路径执行 / adapter 2 次);首动 86.98 s 只作日志数字。入口脚本 mode 只记录不校验,身份看 sha256。

## 变更 14(9/5 02:3x:对外标题与术语;报告/deck v3/README 下一版统一)
- 对外主标题统一为「面向工业机械臂的指令交互型智能体原型 —— 开放词汇感知、确定性任务规划与可审计执行」;"符号契约校验"只作一次括注(内部名)。变更 1 的标题保留为评委包 230159 版之前的历史写法。
- 术语按用户 9/5 02:00 要求:用领域常见中文词(任务规划/规划器/校验器/执行授权凭证/一次性令牌/失效安全/边车容器/运行记录…),内部词只在首次括注;放行"规划/推理/智能体"(见 ruling.report_banned_words)。
- 报告数字来源:CLAIMS-0904/0903、NUMBERS-v2、README §Prerequisites/§Verification、negative-results;S0 "2.12 s" 只能带 live_entry 路径身份引用。
