# 90–110 秒参赛视频脚本(v2:边界分布式,第六轮审查后重构)
维护人:tzb-fe　2026-08-31　**口径以 `CLAIMS-SHEET-20260831.md` 为准,冲突以其为准**

**v1 已作废**。v1 把全部能力边界压在 70–88s 单一镜头,并标"不可删不可弱化"。第六轮审查指出:那是**最先被剪掉的一镜**,而人工标签正是第 19 条(标签不属实)的同一病灶——**标签不能替代结构上的抗剪性**。
v2 改为**边界在四处分布出现**:任何一处被剪,其余仍在。

**版本裁定**:Task32 未确认坐标链根因,**不录任何抓放镜头**(含失败镜头)。
**素材硬约束**:片中每个数字、错误码、字段路径必须可追溯到冻结工件。**禁止为画面构造示意用的计划、错误码或结果。**

---

## 镜头 1|0–12s　问题陈述 + **边界第 1 次出现**

**画面**:工业单元静帧,指令浮出:"把这个零件放到 B 区料箱",下方浮现一段 JSON 计划。
**6–10s 固定角标(边界第 1 次,不可省)**
```
本次验证:符号合同与执行前验证链
未验证:视觉识别、稳定抓放
```
**旁白(32 字)**
> 大模型可以把一句话变成一份计划。但这份计划,能直接交给机械臂吗?

---

## 镜头 2|12–30s　核心见证

**画面**:三段式推进。
```
左:decision = "REFUSE"          ← 高亮
   subtasks: [CARTESIAN_POSE,   ← 红框
              GRIPPER_POSITION]
中:静态 JSON Schema ✓   旧最低期望匹配 ✓
   V2 契约层结构互斥检查 ✗
   STRUCTURAL_MODE_CONTRADICTION @ /subtasks
右:末端原语 dispatcher  调用点未到达
```
**四格必须显示同一个 `case_id` 与 `plan_sha256`**,否则无法排除"换了一份计划"的质疑。
**全程角标(不可省)**:`构造对抗测试案例｜非 Commander 真实事故`
**旁白(54 字)**
> 这份构造的对抗计划写着"拒绝",却夹带了物理动作。格式检查通过,旧的最低期望匹配也通过。V2 运行时契约识别出矛盾——未到达末端原语 dispatcher 调用点。

> **禁止**:说"机械臂保持不动",也**禁止**说"受测执行入口记录 0 个原语"。末端 dispatcher 是 test-only 记录替身且从未被调用;本见证案例更在 Pydantic 契约层即被拒,连 probe 都未进入。唯一允许:**未到达末端原语 dispatcher 调用点**。注意 23 个案例**确实调用了**生产 gate 函数 `dispatch_after_semantic_gate_v2`,故"派发接口未被调用"同样有歧义,一并禁用。

---

## 镜头 3|30–46s　架构 + **边界第 2 次出现**

**画面**:横向架构链逐段点亮。
```
图像·指令 → 27B Commander → 符号技能契约 → 语义验证器 → 执行入口
          → 抓取合成·IK·碰撞·接触门 → Franka
```
**三处必须画死**
1. 粗边界框:`可证明边界:计划字节绑定 → 语义判定 → REJECT 时未到达末端原语 dispatcher 调用点`
2. **视觉输入箭头旁灰标(边界第 2 次)**:`接口存在;本轮视觉能力未形成可解释证据`
3. 运行时重关联握手画**虚线** + `vNext,未实现`
**模型框标注**:`候选符号计划生成器`。**禁止**写"已验证的理解与规划大脑"。
**旁白(52 字)**
> 我们不让大模型直接控制机械臂。它只生成候选符号计划;坐标、轨迹、关节值,全部留给确定性执行层。

---

## 镜头 4|46–58s　正面量化

**画面**:2×2 混淆矩阵 + 一行 `REJECT → 未到达末端原语 dispatcher 调用点`。
```
          放行   拒绝
合法计划    24     0
不安全计划   0    24
```
**旁白(44 字)**
> 四十八个冻结的语义案例:二十四个不安全计划全部被拒,二十四个合法计划全部放行。被拒绝的计划,均未到达末端原语 dispatcher 调用点。

> **禁止**任何全称句("所有危险计划""安全率 100%")。这是**冻结规则、冻结案例、受测执行路径内**的结果。

---

## 镜头 5|58–66s　**边界第 3 次出现**(能力矩阵,8 秒而非 18 秒)

**画面**:两列对照,不加解释文字。
```
已验证                        未完成
─────────────────────────    ─────────────────────
有限任务族的符号合同生成        视觉识别
执行前验证链冻结测试套件        稳定抓放
REJECT 后未到达末端原语 dispatcher 调用点        运行时重关联握手
```
**旁白(28 字)**
> 我们也把没做到的列出来:视觉识别、稳定抓放、运行时重关联握手。

---

## 镜头 6|66–80s　抓取 campaign

**画面**:12 个 ordinal 的实际处置状态(**不画 X 分档、不画阈值带**),数字与画法**只引用** `reports/deck-m2c-campaign-block-20260831.md`。
**旁白 = 前引句 + 冻结原话**

前引句(可编辑):
> 抓取实验方面:

后半 = **冻结原话,字面锁定,逐字符不可改**。
**唯一来源链(不得有第二来源)**:`evidence/campaign/…halted-report-v2.json` 的 `/aggregate_accounting/aggregate_conclusion` → 权威清单 §1 保存**逐字节镜像**(代码块,行尾无句号)→ deck 与字幕由该镜像生成。
> `已完成的 5 个有效结果均未成功;原定 12 次 campaign 未完成,因此不作成功率或假设检验结论`

**口播呈现说明(只影响念法,不改字面)**:数字念"五个""十二次";`campaign` 按原词念或念"实验轮次"。**字幕必须显示上面的字面原文。**
**该行不得由任何人手工誊写。行尾不得添加句号**(冻结件无句号)。**"不可删弱化"这个人工标签本身不构成保证**(第 19 条教训);真正的保证是**包内附带的 exact-match 比较脚本与其运行回执**——在回执入包之前,"exact-match 已通过"仍只是人工声明。

---

## 镜头 7|80–95s　定位收束 + **边界第 4 次出现**

**画面**:交付物栈(左)+ 三项 vNext(右),证据索引入口。**不放长串 SHA。**
**旁白(主句,60 字)**
> 我们交付并验证的,是机械臂前的**执行前验证链**:在本次冻结测试范围内,它放行合法计划、拦截已覆盖的矛盾与越权计划,且被拒计划均未到达末端原语 dispatcher 调用点。

> **统一命名(不得混用)**:整条链叫**执行前验证链**(契约层 + 语义验证器)。**禁止**用"语义执行门"指代整条链——镜头 2 的核心见证正是在 **Pydantic 契约层**被拒的,**根本没有进入语义验证器**。用"语义门"命名整条链,等于把一个不属于它的案例算在它头上。"语义验证器"仍可用于指代该组件本身。

**92–95s 尾字幕(边界第 4 次,2–3 秒)**
```
视觉识别与稳定抓放尚未完成
```

---

## 边界出现位置一览(抗剪设计)

| 次序 | 时点 | 形式 |
|---|---|---|
| 1 | 6–10s | 开场角标 |
| 2 | 30–46s | 架构图灰标 + vNext 虚线 |
| 3 | 58–66s | 能力矩阵 |
| 4 | 92–95s | 尾字幕 |

**任何一处被剪,其余三处仍披露范围。**这取代 v1 的"不可删"人工标签。

---

## 交片检查表

- [ ] 每个数字可追溯至冻结工件,无一为示意
- [ ] 镜头 2 四格显示**同一** `case_id` 与 `plan_sha256`
- [ ] 镜头 2 角标"构造对抗测试案例｜非 Commander 真实事故"全程可见
- [ ] **全片无"机械臂保持不动"、无"受测执行入口记录 0 个原语"**,一律"未到达末端原语 dispatcher 调用点"
- [ ] **全片无全称句**:"任何/所有不安全计划""安全率 100%""保证安全"
- [ ] 镜头 6 冻结原话与 halted 报告字段 **exact-match 通过**,且**比较脚本与运行回执已入包**(非人工声明);行尾无句号
- [ ] 四处边界**全部在片**
- [ ] 无 `0/12`、"二项检验"、"正式阴性结论"、"Schema 原生三路由"
- [ ] 无 `−44.3%` / `−66.4%`
- [ ] 无任何抓放镜头(成功或失败)
- [ ] 无点名或影射其他参赛队伍
- [ ] 总时长 90–110 秒(本版布局约 95 秒)
