# 我们的实际现况 — XH-202607(截至 2026-08-31)

**发给你的目的**:你上一版三根钉子的方案是按"手上没有资产的队伍"写的。我们不是那支队伍。下面是逐项实况,**每个数字都带它的范围**,好让你的下一轮建议落在我们真实的位置上。

**本文纪律**:所有数字都注明测试介质、条件与不支持的外推。凡我们没测过的,写"没测过";凡有已知泄漏的,泄漏和数字写在同一行。这不是谦虚,是因为我们今天一天之内被外部审查和内部 lane 拦下八次以上,全部是同一形态——**证据是真的,贴在它上面的谓词是错的**。

---

## 0. 硬约束

| | |
|---|---|
| 代码冻结 | **9/3** |
| 提交 | **9/5** |
| 主办方规则 | **推荐仿真器,实机不做要求**;院内已部署常见开源大模型,参赛队把 api 地址和 key 写进配置项;测试机有网但不能访问境外 |
| 题目 | 工业环境下**物体感知识别**与**指令交互**型智能体研发;原文明确"机械臂仅作为执行机构,**重点考查智能体的感知决策能力**" |

**用户已定重心:意图感知。VLA 线已停在可行性验证,不再投入。**

---

## 1. 我们已经验证并冻结的(有字节、可复核)

### 1.1 执行前验证链 — 这是我们最强的正面证据

冻结的 48 例语义案例套件(`SYNTHETIC_FREE_CONTACT` harness),24 合法 / 24 不安全(含 12 复合对抗):

```
不安全计划误放      0 / 24
合法计划误拒        0 / 24
错误字段定位        44 / 44
被拒计划            24 / 24 均未到达末端原语 dispatcher 调用点
```

**必须同时说的限定**:
- 这是 **synthetic / free-contact harness**,不是真实接触或物理执行;
- 末端 dispatcher 是 **test-only 记录替身**;23 例经生产 gate 函数测试路径提前返回,**1 例在 Pydantic 运行时契约层前置拒绝、连 gate 都没进**;
- 上述结果是 **2026-08-31 按已核验冻结输入确定性重导出**,不是 2026-08-30 原始运行回执;
- **禁止任何全称句**。唯一允许的量化范围是:"在冻结规则、冻结案例与受测执行路径内,**已覆盖的**不安全计划被阻断。"我们自己披露了 **6 类真实未覆盖规则**(`UNKNOWN_TASK_TARGET`、`UNREGISTERED_TASK_DESTINATION`、`UNKNOWN_TASK_POSE`、`EXECUTION_RESOURCE_CONFLICT`、`RECOVERY_AUTOMATON`、`UNEXPECTED_CAPTURE`),既然列了没覆盖的,就不能同时声称覆盖全部。

验证器规则库存 38 类,套件实际触发 26 类(26/38)。

### 1.2 意图侧的逐族成绩 — 这是与题目最相关的部分

MANUAL_BLIND 留出集,n=45,**逐行重数核过**:

```
CONTRACT_REFUSAL                13 / 13     判断意图无法满足 → 拒绝
REGISTERED_SYMBOLIC_RELOCATION  11 / 11     把搬运意图落到已注册对象与目的地
RECOVERY_EXHAUSTION_REPLAN       7 / 9      失败后重新推导意图
VISUAL_CONDITION_SELECTION       0 / 12     把意图落到视觉条件
```

SYNTHETIC_TEST,n=22:`3/3`、`10/10`、`6/9`(无视觉族)。

**这一节的限定是本文最重要的一句,请特别注意:**

> 示例选择策略是 `two_shot_family_first_v2`,它用 `row.family` 挑同类示例,而 **`family` 就是意图类别、是 gold 标签**。
> 所以上述数字的准确读法是:**"在已经告诉模型这是哪一类意图之后,它能生成正确的合同。"**
> **这是合同生成能力,不是意图感知能力。** 绝对准确率(TEST 86.4% / BLIND 68.9%)必须标注为"示例选择有利条件下的估计"。

`0/12` 不是模型视觉能力的结论:该任务**统计上不可识别**——期望答案要求逐字输出输入中完全不存在的内部 ID。任何仅访问当前公开输入、且无外部 ID 映射或泄漏的方法都取不出它。这是**任务缺陷**。

### 1.3 权重 vs 编排

同一 ICL harness 内:`zero-shot 18/78` → `family-conditioned two-shot 64/78` → `挂 adapter 后仍 64/78`。

**限定**:前一差值**混合了示例内容与答案相关的 family 路由**,不能解释为编排层的因果增益。可作部署决策的结论**只有 adapter 增量为 0**。
**并且 `18/78` 仍在 ICL harness 内,不是裸基座。**

### 1.4 微调:实测无可主张收益

two-epoch 完整执行(856 microsteps / 54 optimizer updates / 12/12 promotion gates)。成对评测(`expectation_match` 指标):

```
SYNTHETIC_TEST  19/0/0/3   McNemar discordant = 0,p = 1.0,cluster = 2
MANUAL_BLIND    31/0/0/14  McNemar discordant = 0,p = 1.0,cluster = 3
DEV             63/1/1/13  仅 supporting,两臂都见过 DEV
```

唯一差异:MANUAL_BLIND 上 `strict_valid` 与 `reassociation_boundary_pass` 从 `44/45` 升到 `45/45`,**恰为 1 例视觉族案例**——BASE 输出空 `subtasks` 未显式 REFUSE 触发契约错误,LoRA 输出结构合法但目标引用错误的计划。**该例两臂 `expectation_match` 同为 False,没有把任何题从错变对。**

报告字段:`benefit_claim_authorized = false`、`primary_point_gate_pass = false`、`practical_difference_minimum = 0.05`、`stability_flags = [UNSTABLE_LOW_CLUSTER_COUNT]`。

**对外唯一允许的短句**:"**主指标未观察到变化;不证明非退化。**"
**禁止**:已证明非退化 / 能力没有下降 / adapter 与 BASE 等价。cluster 只有 2 和 3,区间被显式标为不稳定,`primary_ci_not_negative = true` **不构成非劣性证明**。

### 1.5 实现回归

**在冻结语义契约路径的七文件回归子集上,135 项全部通过**,并另行通过 lint / compile / schema / diff 四门(四门不在 135 内)。

**全仓数字是日期依赖的,这一点我们是 2026-09-01 复跑时才发现的:**
```
2026-08-31   14 failed / 1933 passed / 5 skipped   ← stdout 未捕获,PROJECT_REPORTED,未被我们重现
2026-09-01   61 failed / 1886 passed / 5 skipped   ← 有 记录的命令/stdout/nodeid/退出码回执
             (两次均为全仓扣除一个被 ignore 的 collection-error 文件)
扫描范围内    无 source/test/config 文件 mtime 晚于 8/31 17:00(扫描返回 3 条:文档 + 两个 pytest cache)
```
**候选解释**(不是已确立的根因)是我们**自己预注册的硬冻结门**在 9/1 零点生效(`M2C_HARD_FREEZE_LOCAL_ISO = 2026-09-01T00:00:00+08:00`)。

**这一段我们改过三次,每次都是被内部 lane 拦下的,过程本身可能比结论有用:**
- 第一版写"47 个新增失败全是实验执行路径,被该门正确拒绝" → **19 个失败文件里只抽查了 1 个**,一个样本不能为 47 个结果确立成因。
- 第二版写"仓库一个字节都没变" → **`find -newermt` 是 mtime 证据不是字节证据**,范围还排除了 `.git`/`.venv`/`artifacts`,而且**扫描结果本身非空**(我们自己的证据文件里就列着一条变动)。
- 第三版写"它永远无法再复现" → **从一次观测推出了对所有未来日期的断言**。
- 第四版改成三条件的条件句 → **仍然过强**。我们今晚刚因为"断定不可恢复"错过一次(七文件 selector 就在 transcript 里),**而我们根本没去 transcript 里找过 8/31 的 stdout**。现行说法只到 `NOT_IN_PACKAGE_EVIDENCE` 与 `NOT_REPRODUCED_BY_US`。

**现在能说的**:数量差 47 出现在冻结边界;在 find 的扫描范围内未发现 mtime 晚于 2026-08-31 17:00 的 source/test/config 路径(这是局部 mtime 观测,不证明 mtime 未变化,更不证明字节相同);门机制在**一个抽样失败文件**上演示。**未确立**:那 47 个是否全部由该门导致。

**61/61 全在 `test_m2c_*` 治理 lane,没有一个属于 `qwen_brain`;`qwen_brain` 今日实测 359 passed / 4 skipped / 零失败。** 我们**没有逐个诊断**那 61 个,所以不说"它们无害"。

**关于 `135`:2026-09-01 已闭合。** 我们一度判定七文件 selector 不可恢复(试跑三文件子集得 69 而非 135,随即停止不猜)。**那个判断过早了**——记录一直在产生它的 transcript 里。该 lane 从 tool-use 与 tool-result 事件精确恢复了有序七文件与 argv(零猜测、零试组合、零重跑),我们用恢复出的 argv 独立实跑得 `135 passed` / exit 0。**tzb-fe 于 2026-09-01 按恢复的 `exact_argv` 独立实跑,观察到 `135 passed`、exit 0**(只报这一次观测,不作"可复现"的一般性主张)。 边界:恢复的是 **selector**,不是 P1 时点的文件身份。

---

## 2. 我们没有的

### 2.1 抓取:诚实的中止

冻结账目原话(逐字节,行尾无句号):

```text
已完成的 5 个有效结果均未成功;原定 12 次 campaign 未完成,因此不作成功率或假设检验结论
```

组成:5 有效结果 / 0 成功 / 7 未消费 / **6 个从未执行** / ordinal05 曾发生一次真实物理派发尝试(SSH 已发、worker 已启动、runner `rc=1`)。

**因此视频里一个抓放镜头都没有**(成功或失败都没有)——坐标链失败的根因至今未确认。

### 2.2 视觉识别:无可辩护的正面证据

见 1.2。P3 因永久硬边界(**特权仿真真值不得进入 test-time policy input**)取消。

### 2.3 裸基座基线:从未测过

**我们无法证明系统在帮忙,而不只是在加约束。** 这是我们自己列的 vNext 第一优先。

### 2.4 运行时重关联握手:明确未实现

### 2.5 跨基座替换:从未测试

架构通过符号合同接口分层,**但不声称可替换性已验证**。

---

## 3. 我们手上的工程资产(你上一版低估了这块)

- **Isaac 管线已在跑**:`isaac_dataset_v1_pilot`(50 scene / 12 帧每 scene / 550 episode 目标 / 按 scene_seed 切分)、失败富集场景生成器、公开失败谓词、几何 RGB-D 感知模块。**你说"Isaac 对没摸过的人是三天填不满的坑"——对我们不成立。**
- **契约层与语义验证器**:Pydantic V2 typed/runtime 结构契约 + 语义验证器 + 计划哈希绑定 + TaskSpec/world-snapshot 绑定 + expiry。
- **模型**:Qwen3.8-27B 多模态,**未加载任何 adapter**,已 commit + tag 冻结。ModelScope 上可取(已实测 200,带阴性对照)。
- **VLA 可行性(已停)**:MolmoAct2-DROID 管线连通性测试通过(20/20 结构合法),但**结论是连续动作盾今天只有 spec、没有可运行代码**。已停在这里。

---

## 4. 我们自己诊断出的、尚未修的缺陷

**这一节是给你判断"该往哪加"的关键输入。**

1. **路由泄漏**:`family` 是 gold 标签却被用于选示例。**重心转到意图感知后,这不再是脚注,而是主题本身。**
2. **视觉任务不可识别**:期望答案要求输出输入中不存在的 ID。
3. **歧义覆盖近乎为零**:48 例套件里 `AMBIGUOUS_REQUEST` **只有 1 例**。而"知道自己没搞懂并开口问"正是意图感知的核心能力之一。
4. **无裸基座对照**(见 2.3)。
5. **任务族单一**,统计上不可识别的问题已经出现过一次。

---

## 5. 我们想问你的

前提:**冻结 9/3,现在改动 48 例套件会作废全部冻结证据**。所以下面分两层。

1. **给定 1.2 那个泄漏,我们现在还能对外声称什么?** 我们的当前口径是"给定符号任务状态后,模型能生成期望合同",并明令禁止升级为"工业环境推理能力"或"自主任务路由"。**这个天花板压得对吗?**
2. **重心定为意图感知之后,你那三根钉子要不要重排?** 我们的判断是钉子一(把提问纳入信息价值决策、KnowNo 式的问人阈值)升到第一,钉子二偏物体感知,钉子三支撑恢复。**你同意吗?**
3. **歧义与提问**这个空缺(1/48),值不值得作为下一个里程碑的主线?它纯符号、不碰硬件、直接打在题目上。
4. **申报书里怎么摆 1.1 与 1.2 的关系**——我们已验证的是"执行前验证链",题目要的是"感知决策"。我们认为诚实的讲法是:**验证链是那三根钉子的先决条件,不是替代品**。这个框法站得住吗?
5. 你上一版里"**据我所知是组合空白**"这类措辞,我们不会写进申报书,只会写"检索后未找到同时结合 X 与 Y 的工作"。**如果你认为某一项确实是空白,请给出你检索的范围**,我们要能复核。

---

## 6. 一句关于本文可信度的话

本文第 1、2 节的数字经过至少两轮独立复核(协调会话 + 执行 lane 交叉核验),原始冻结件的摘要与包内拷贝逐字节一致;全仓数字由本人两次实跑并留有记录的 shell 命令 / 原始 stdout / nodeid / 退出码回执(见 §1.5 的日期依赖更正)。**第 3 节的 Isaac 管线参数是从配置文件单次读取,未经二次核验**——这类区别我们逐条标出来,而不是笼统说"都核过了"。但**我们今天记录在案的同形错误已超过八次,其中约半数由协调会话本人造成**,形态全部是"证据没错,赋予它的谓词、范围或组件归属错了"。

所以:**如果你在本文里发现任何一句话的范围写得比证据宽,请直接指出来。** 那比给我们新点子更有价值。
