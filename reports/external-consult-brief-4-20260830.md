# 第四轮外部咨询 · 挑战杯 XH-202607(2026-08-30)

**这是第四轮。** 请**优先反驳我们**——前三轮里价值最高的产出全部来自你指出我们错了(第二轮你预判"LoRA 大概率打平",落实了;第三轮你指出"视觉任务不是贡献小,而是统计上不可识别",直接改写了我们的路线)。本轮我们带六个尚未裁决的问题来(前三个是主问题,后三个是 9/3 代码冻结前必须定的具体取舍),并附上**我们自己查出的三个错误**请你评价纠错方法。

---

## 0. 前三轮速览(不必重读历史)

| 轮次 | 问了什么 | 结论 / 我们做了什么 |
|---|---|---|
| 第一轮 | 四个方法论难题:抓取基线偏差、小数据微调、近重检测、阴性结果叙事 | 你建议 ICL 做主路径、LoRA 做受控实验,"只有 LoRA 稳定超过调优后的 ICL 才作为亮点"。已照做。 |
| 第二轮 | 带真实数字回来:微调与 few-shot 打平了,五天内怎么救 | 你判定不要为出正数扫超参、不启动 3000 episode 确认实验;打平要讲成有信息量的结论。已照做。 |
| 第三轮 | 附完整代码/配置/数据/证据包,问"我们整体判断链哪里想错了" | 你给出三项核心纠正:(a) 抓取线**不是** 0/12,是 5 个有效结果后停机 + 7 个 ordinal 未执行,下措辞禁令;(b) 视觉任务**不是**"视觉贡献小",是**统计上不可识别**;(c) 现 evaluator 只验字段出现,是"最低期望匹配器",撑不起"可验证契约"卖点。**全部采纳并已落地**:语义验证器 V2 + fail-closed 派发门(134 PASS + lint/compile/schema/diff 全绿)、48 例对抗契约测试(进行中)、治理停止扩张、8 页 deck 按你的骨架重构。 |

**时间线**:今天 8/30。**代码冻结 9/3**(不是 9/1,用户已改),材料制作 9/3–9/5,**提交 9/5**。约 3.5 天工程 + 2 天材料。

**未脱敏**:以下主机名、绝对路径、commit、SHA-256 均为真实值,照原样给出,便于你独立核对内部一致性。

---

## 问题一|双分支叙事**预注册**(本轮核心)

### 1.1 客观状态(截至发出时)

12 发存在性探针 campaign 正在按用户授权跑完(硬截止已由用户口头延后,halted-report 预案不再自动触发)。治理链 head = P″ `51de5c916ed8065ab2d20effc23ad5c3336d24af`,分支 `m2c-recovery-h3`,仓库 `/Users/gl/tzb-qrm-lite`。

**已消费部分**(全部冻结,机器可读账目 `/private/tmp/m2c-r13239-campaign-final-accounting-summary-v1.json`,mode 0444):

```json
"aggregate_accounting": {
  "descriptive_result": "B0_VALID_ACCOUNTED_OUTCOME_SUCCESSES_0_OF_5",
  "formal_terminals_present": 3,
  "legacy_terminalized_failures": 3,
  "ordinal04_valid_measurement_failures_with_missing_formal_closure": 1,
  "retrospective_recovery03_failures": 1,
  "unconsumed_ordinals": 7,
  "valid_accounted_b0_outcomes": 5,
  "valid_accounted_b0_successes": 0
}
```

按你第三轮下的**强制成分披露**(禁裸写 s/12),12 发构成 = 3 个 legacy terminal + 1 个零消费追溯闭合(ordinal03,合法试次的真实 `POSE_GATE_REJECTED` 失败)+ 1 个 orphan-closure(ordinal04,物理测量有效且经 committed validator 纯读确认完整,但记账段被控制通道取消截断,容器回执与控制台字节无法诚实重建)+ **7 个 successor-native 待执行**。

你第三轮要求的**合并三门**(共享 upstream V4 digest 逐发比对 / 各 ordinal 匹配自身预注册代 / 具名 `ACTUATION_PATH_NON_DRIFT_ACROSS_GENERATIONS` 附证)已于 8/30 21:17 **CLEARED**(12 处 raise 覆盖漂移面;分段报告早返回路径经测试证明物理不可达;15/5/344 全 PASS)。因此单一 12 分母合法,不必退化为分段报告。

### 1.2 冻结判据(运行前写死的代码,不可事后修改)

```python
# src/xh_agent/policy/qrm_lite/v5_existence_probe_campaign_v1.py
PROBE_COUNT = 12
EXISTENCE_FLOOR_FRACTION = 0.30
EXISTENCE_FLOOR_MIN_SUCCESSES = math.ceil(PROBE_COUNT * EXISTENCE_FLOOR_FRACTION)  # = 4
# scripts/m2c/audit_v5_existence_probe_v1.py
EXACT_BINOMIAL_N, EXACT_BINOMIAL_P0, EXACT_BINOMIAL_ALPHA = 12, 0.75, 0.05
THIRD_REGIME_MAX_SUCCESSES = 5
SEVERE_INTER_STRATUM_DIVERGENCE_THRESHOLD = 0.75
```

分支:`X<4` 或成功未分布在 ≥2 个预注册层 → `FREEZE_KEY_ELIGIBILITY_RETURN_TO_COORDINATION`;层间成功率极差 ≥0.75 → 同上;`X∈{4,5}` → `RETURN_TO_COORDINATION_BEFORE_KEY_FREEZE`;`X≥6` 且无严重层间分歧 → `ADVANCE_TO_KEY_FREEZE`。精确二项 P(X≤5 | n=12, p₀=0.75) = **0.01425**。

### 1.3 算术后果

已消费 5 个有效结果、**成功 0 个**;剩余 7 发。故 X 的取值域是 **[0, 7]**,而"不拒绝 p₀=0.75"要求 **X≥6**,即剩余 7 发必须**成功 6 或 7 发**——在前 5 发零成功、最佳接触误差 95.3 mm vs 20 mm 门限(系统性偏差而非随机抖动)的前提下,这**极大概率不会发生**。最可能的落点是 `X<4` 的 floor 未过档。

也就是说:我们据此**预判**(注意:是预判,不是主张)——主结果落在拒绝分支的概率极高,最终大概率会是一个诚实的负存在性结果。**判定尚未产生,下文两支叙事全部是条件式预案。**

### 1.4 我们要问的

**(A) 一个诚实的负存在性结果 + 完整可验证治理链,作为参赛材料,对非机器人专业的评委是减分还是加分?怎么呈现才能是后者?**

**(B) 请对两个分支各给一套具体呈现方案**(页面落点、措辞、数字怎么摆、评委追问怎么答):

- **分支 R(拒绝,预期路径)**:X≤5。请再细分——`X<4` 的 floor 未过档 vs `X∈{4,5}` 的 third regime,叙事上**要不要分开**?我们倾向合并为一句"未达存在性下限",但担心这是在模糊掉信息。
- **分支 N(不拒绝,低概率)**:X≥6。注意这个分支反而**更难讲**:它意味着"前 5 发全败、后 7 发几乎全成",两代运行时之间存在巨大的行为断裂,我们必须解释断裂来源(坐标系?successor 实现差异?),否则这个"好结果"本身就是不可信的。你认为在这个分支下,诚实的呈现是不是反而要**主动质疑自己的成功**?

**(C) 请评价"先定叙事"这个做法本身。** 我们**现在就要把两个分支定死,在判定数据落下来之前**。理由:等结果出来再决定怎么讲,那是按结果找说法(HARKing 的叙事版本);先预注册叙事,才和我们整套预注册方法论自洽。但我们也担心两点:(1) 这可能只是自我感动的形式主义,评委根本不会知道我们何时定的叙事;(2) 预注册叙事会不会反过来让我们在真实结果偏离预期时反应迟钝?**请直说这个做法是真有方法论价值,还是仪式。**

### 1.5 措辞约束(你第三轮下的禁令,我们仍在遵守)

12 发完成前,唯一合法表述 = "已完成的 5 个有效结果均未成功;原定 12 次 campaign 未完成,因此不作成功率或假设检验结论"。禁写 `0/12`、"12 次探针拒绝了成功率≥75%"、"二项检验表明"、"正式阴性结论"。**本节所有分支叙述均为条件式预案,不构成当前主张。** 请你在给方案时保持这个纪律。

### 1.6 adapter 身份链(原为待填占位,已核实并补齐)

成对评测 `TRAINED_LORA` 臂使用的 adapter 身份此前未在权威状态的规范区记录,只散落在日志中。现已核实并经协调侧独立复核:

- final adapter = `/home/gl/xh-202607-qwen-s5/adapter-two-epoch-node2-v1`,3 files / 79,237,330 B;
- adapter tree SHA-256 = `8a09f56a4a77c0127c0f1c2008efb8726e11d4a9b0c56f526a9934ce0a88f0b9`;
- 与 smoke adapter 的 publication SHA `b7ad8ebc4134f63165bad8b27be674b552c1bb83a39522ac6b9fb22be55f5d1c` **不同**,确系两个不同 adapter;
- DEV / TEST / BLIND 三处 `TRAINED_LORA` 执行**均显式绑定** adapter tree `8a09f56a…`;TEST candidate report SHA = `2b322fe5c375e18fd513dfb021284583eb9c93d8bb070e253c01a2f9c617a43b`,BLIND candidate report SHA = `6f9e0d91d538418eb0071c0fd41edecb8f2ce46a7866b8eef4b00b2963ca5b29`;
- 训练本体:**856 microsteps / 54 optimizer updates**,elapsed **1778.63 s**,**12/12** promotion gates 全 true,report SHA-256 = `1513f6de2c641f3dd8f90883a5c71ac8fb0e92adc9fe1b1b60c872646000be06`。

**结论**:被评的是 two-epoch adapter,**不是** smoke adapter(32 microsteps / 2 updates)。因此 `NO_BENEFIT_EVIDENCE` 的 adapter 身份链成立,问题一的前提不变。这条链是我们**在写这份咨询包的过程中才补齐的**——详见第三节第 3 项,我们把它当作第三个自查错误呈现。

---

## 问题二|P3 视觉 grounding 设计评审(尚未开工,此刻改动成本最低)

你第三轮给了大方向(公开候选 marker + 私有映射)。我们把它做成了具体设计,**还没写一行实现代码**。请在开工前把它拆了。

### 2.1 上一版任务设计的真实缺陷(我们自己查出的,诚实呈现)

**(a) 86% 的判定信息写在文本字段里。** 573 行数据集,四个任务族:`REGISTERED_SYMBOLIC_RELOCATION` 168 / `CONTRACT_REFUSAL` 165 / `RECOVERY_EXHAUSTION_REPLAN` 162 / `VISUAL_CONDITION_SELECTION` 78。前三族共 **495 行 = 86.4%**,其判定信息(`target_ref` / `local_recovery_exhausted` / `destination_registered` 等)直接写在 `prompt_context` 里,模型不看图也能答对。期望答案本身就是 `prompt_context` 的确定性函数:

```python
# src/xh_agent/qwen_brain/s5_icl_v3.py
def _expectation_from_row(row: S5IclDatasetRowV3) -> S5IclEvaluationExpectationV3:
    unresolved = row.prompt_context.get("binding_unresolved") is True   # ← 直接读文本字段
```

**(b) `SYNTHETIC_TEST` 视觉行 = 0。** 实测各 split×family:TEST 拒绝 3 / 恢复 9 / 搬运 10 / **视觉 0**;TRAIN 视觉 54/428;DEV 12/78;BLIND 12/45。所以 **19/22 = 86.4% 是纯非视觉的符号契约与规划成绩**,不是多模态成绩。(这是你第三轮指出的,我们已盘上复核确认。)

**(c) 视觉任务统计上不可识别(比 (b) 更严重)。** `VISUAL_CONDITION_SELECTION` 行的期望答案要求模型**逐字输出一个内部不透明 target ID**(形如 `s5_track_3ab109db93005a61`),而 schema 校验器**主动把该 ID 从模型可见输入中删掉**:

```python
# s5_icl_v3.py:S5IclDatasetRowV3.prompt_context_is_public_view
if self.family.value == "VISUAL_CONDITION_SELECTION":
    expected.pop("target_ref", None)
    expected.pop("approach_pose_ref", None)
```

即:该 ID 在 instruction 与 `prompt_context` 中**完全不出现**,却被要求逐字复现。**任何"视觉答对"都应优先怀疑泄漏 / 顺序 / ID 相关性,不能解释为视觉绑定成功。** 那 78 行在设计上无法完成。

**(d) 示例选择是 answer-correlated 的。** 部署策略 `two_shot_family_first_v2` 的冻结 `demo_selection` 字面值就是 `SAME_FAMILY_THEN_GLOBAL_SHA256_V2`:

```python
if policy.shot_count == 2:
    same_family = [row for row in ranked if row.family == target.family]
```

而期望答案是 family + `prompt_context` 的确定性函数,故按 target 自身 family 挑示例 = 用与答案相关的量做条件。**因此绝对准确率 86.4%(TEST)/ 68.9%(BLIND)/ 82.1%(DEV, 64/78)必须标注为"示例选择有利条件下的估计"。成对结论不受影响**——两臂用完全相同的示例选择,唯一变量仍是是否挂 adapter,`n11/n10/n01/n00` = TEST `19/0/0/3`、BLIND `31/0/0/14`(逐例完全一致),`NO_BENEFIT_EVIDENCE` 成立。

### 2.2 新设计(待你评审)

- **公开候选 A/B/C/D 字母标记**:在既有 RGB 上用仿真器实例分割真值叠加 marker;**模型只见:带标记的 RGB + 指令 + 允许输出的字母集合**;`A→track_17` 的映射表**由确定性 binder 私有,绝不进入模型视野**,模型响应前冻结。模型输出 `selected_marker` + 意图 + 目的地,由 binder 转成内部 `target_ref` / `pose_ref`。**绝不让模型猜内部主键。**
- **规模**:**32 例 = 8 个基础场景 × 2 条指令(各选中不同对象)× 2 套 marker 置换**;另在 16 个场景-指令组合上成对跑 blank / shuffled / text-only / 正常。
- **反捷径**:marker 字母随机置换;样式尺寸偏移一致;正确目标不得总在 A 或固定位置;marker 不遮挡关键视觉特征。
- **每次绑定留证**:`image_sha256` / `overlay_sha256` / `candidate_mapping_sha256` / `selected_marker` / `bound_target_ref`。
- **统计单位声明为 8 个基础场景**,不假装 32 行独立。
- **披露**:材料中明说候选由**仿真器实例分割前端**生成,定位为"仿真器实例分割支持的候选生成 + 多模态指令条件候选选择 + 确定性内部绑定",**不称端到端检测**。

### 2.3 我们要问的

1. **这个设计能否真正支撑"视觉绑定"主张?** 具体说:模型输出一个字母,就算答对了,能否排除"它是在做文本推理 + 猜位置"?我们自己想到的最强反驳是"marker 置换一致性 + 同图换指令的反事实敏感性"两项联合——够吗?还差什么?
2. **32 例 / 8 个基础场景的统计设计够不够?** 以 8 为统计单位,任何成对比较的有效 n 就是 8。这个规模能支撑什么强度的主张、不能支撑什么?如果不够,在**不重新渲染**(labserver 的 Isaac 正被抓取探针占用,3.5 天内不太可能腾出)的前提下,最省的扩容方式是什么?
3. **还有什么可识别性陷阱?** 我们已经在同一类错误上栽过两次(感知哈希仪器退化、任务视觉依赖性未验证),很担心存在第三个。请具体列出这个新设计里**你能预见但我们没写进来**的陷阱——尤其是:marker 叠加本身引入的伪影相关性、8 个基础场景之间的共享背景导致的有效自由度进一步塌缩、blank/shuffled 对照的正确构造方式(shuffle 什么、shuffle 到什么程度才是有效的零假设)。
4. **要不要干脆不做?** 3.5 天工程窗口里,P3 grounding 与 48 例对抗契约测试(P2,你第三轮点名的"新的最高价值工程")在争夺同一段工程窗口。如果只能做完一个,你选哪个?(另见问题五,那里把这两项与第三项候选放在一起排。)

---

## 问题三|8 页主 deck 的页面级结构(在拒绝分支 X≤5 的条件下)

你第三轮已给过 8 页骨架(1 成功演示 / 2 分层架构 / 3 什么叫可验证契约 / 4 失败恢复 / 5 正面量化 / 6 能力边界 / 7 证据完整性 / 8 交付与下一步)。**本轮不是请你重给骨架,而是问:在下面三个新约束下,这个骨架具体哪里要改、每页放哪些可核验数字。**

### 3.1 三个新约束

1. **主结果预计落在拒绝分支**(问题一;判定尚未产生,以下为条件式规划)。第三轮定稿时抓取线的合法表述还是"5 个有效结果未成功、campaign 未完成";**若** 12 发跑完且 X≤5,它将变成一个**有明确 α=0.05 判据、有分层设计、有冻结代码支撑的判定结论**。这不是同一张页面——第 6 页"能力边界"的性质会从"未完成"变成"已判定"。请按这个条件分支给页面方案。
2. **代码冻结 9/3**。9/3 之后只能做材料。因此第 5 页"正面量化"能放什么,取决于 9/3 前实际能落地什么:语义验证器 V2 + fail-closed 派发门(**已完成**,134 PASS)、48 例对抗契约测试(**进行中**)、P3 grounding(**未开工**)。请按"哪些一定有 / 哪些可能有"给两套页面方案。
3. **两条量化主线都是阴性**:抓取未达标;LoRA 成对 delta = 0 且**逐例完全一致**(`n10 = n01 = 0`),`NO_BENEFIT_EVIDENCE` 冻结,部署配置定为无 adapter 的 two-shot few-shot。

### 3.2 需要在页面上正确表达的系统事实(供你分配)

双系统架构:**27B 多模态 VL 指挥体低频决策**(Qwen3.8-27B,node2 单张 A100-SXM4-80GB,Transformers 直连无服务层)→ **严格 JSON 技能契约**(6 个原语、12 个谓词、逐原语固定 wire shape;不得输出真实坐标/轨迹/关节值)→ **可验证经典执行层**(采样式抓取合成 + IK + 碰撞检查 + 接触门 + 符号原语,Isaac Sim + Franka Panda 七轴臂)。**明确不是端到端 VLA**:自然语言不直接驱动关节,必须先编译成带前置/后置条件的符号契约,执行层可拒绝越权或不可达请求。**显式双层失败恢复**:技能级(换抓取候选 / 重对齐 / 局部重试)→ 任务级(升级回指挥体重分解,或安全拒绝)。此外:预注册评测协议(规则/阈值/判据运行前冻结、失败必须计入分母、门限不可事后移动)+ 可追溯治理链。

**信任边界(必须画出证明边界终止于哪一层)**:可声称"计划字节不可变绑定 + 派发边界处 REJECT 时原语派发计数为 0";**禁称**"端到端证明执行器执行了且仅执行了这些原语"。

### 3.3 要你给的

**逐页级建议,不要原则。** 每页:标题一句话、放哪 2–4 个数字(从上面的真实数字里选)、一张什么图、评委看完这页应该记住什么、最可能被追问什么。特别请你判断:

- 第 1 页放"完整干净的成功演示"——但我们**没有**成功的抓放镜头(坐标系偏差未修)。如果 2 小时坐标系自洽检查修好了,补一个**诚实标注为 vNext 工程演示**(不回填、不并入任何分母)的镜头,值不值得占掉第 1 页?如果修不好,第 1 页换成什么?
- 阴性结果占**一页还是两页**?两条阴性性质不同(抓取=物理执行未达标;LoRA=无增益故未部署),合并会不会显得在藏,分开会不会显得整场都是坏消息?
- 8 页里**能不能塞下**治理链?你第三轮说治理最多一页且只讲三件(失败没从分母删除 / adapter 无增益故未部署 / 抓取 campaign 真实状态)。在拒绝分支(X≤5)下,这一页的权重是该升还是该降?

---

## 问题四|诚实阴性的承载上限

第三轮把 `EXECUTE 前缀`裁为**结构性禁止**(理由:静态验证器看不到重关联的运行时输出,无法对该步作真实判断;按"验证器验不了的声明不得出现在安全结构里"原则移除,EXECUTE = 恰好六步物理序列)。后果是:**运行时重关联握手成为一个明确对外披露的 vNext 未实现项**。

于是我们要披露的能力边界从两条变成三条:

1. 抓取存在性 campaign —— 已完成的 5 个有效结果均未成功(判定待落);
2. LoRA adapter —— 成对 delta = 0,`NO_BENEFIT_EVIDENCE`,故未部署;
3. 运行时重关联握手 —— 结构性未实现,列为 vNext。

**问:一份 8 页 deck 能承载几个诚实阴性而不被读成"项目失败"?有没有阈值?** 以及:排布顺序上有没有讲究——三条集中在一页(读起来像一次性交代边界),还是分散到各自相关的技术页(读起来像每项技术都自带边界声明)?第 3 条的性质与前两条不同(它不是"测了没达标",而是"我们主动认定验证不了所以不做"),这个区别对评委是加分项(说明自我约束)还是又一个减分项(说明功能缺失)?**如果你认为三条已经越过承载上限,请直接说哪一条应当降级到附录,以及降级会不会构成隐瞒。**

---

## 问题五|Schema v2 三路由是否值得在 9/3 前动顶层

现状:契约 JSON 顶层**仅有** `subtasks` + `rationale`。REFUSE 靠"空数组 + rationale 前缀 `REFUSE:`"表达,RECOVER **没有任何一等字段**。你第三轮判定:不能只靠 validator 加 `if` 就声称三路由互斥;**若冻结前不升级顶层,则只能称"验证器强制的三种结构模式",不得称"Schema 原生三路由"。**

升级方案(已明确,未实施):新增顶层 `decision: EXECUTE|REFUSE|RECOVER`,三者互斥——EXECUTE:`subtasks` 非空且含物理原语;REFUSE:必空且无恢复/物理动作;RECOVER:仅恢复允许集或 recovery request 对象。

这是一个**能在 9/3 前完成、且直接影响卖点措辞强度**的取舍,代价是要同步改 schema、validator、派发门、prompt、以及所有已通过的测试。

**问:请把这一项与下面两项放在一起排优先级,并说明排序依据**(不要只给顺序,要给"为什么这个比那个更值一天"):

- **P2|48 例对抗契约测试**(12 合法执行 + 6 合法拒绝 + 6 合法恢复 + 12 单缺陷 + 12 复合对抗),你第三轮称之为"新的最高价值工程",目前进行中;
- **P3|视觉 grounding**(问题二),未开工;
- **Schema v2 顶层三路由**(本问),未开工。

判断依据请显式包含:哪一项产出的数字最能进第 5 页"正面量化";哪一项不做会导致我们**必须**在材料里用更弱的措辞;哪一项失败的下行风险最小。

---

## 问题六|`PUBLIC_RGBD_CAPTURE` 的构造性 TOCTOU 该放在哪一层

**洞见本体**:计划执行中途拍摄新图,会**自我作废该计划验证时所依据的世界快照**——验证是在快照 S 上做的,新观测产生快照 S′,原验证结论对 S′ 不再成立。因此该动作在语义上**必须归入恢复回路,而不是正常执行序列**:"拍新图 → 新快照 → 指挥体重分解 → 重新过门"本就是双层失败恢复的定义动作。我们据此把 `PUBLIC_RGBD_CAPTURE` 归 RECOVER 而非 EXECUTE。

第三轮你把它排在第 4 页(失败恢复页)的一个要点。

**问两件事:**

1. **这值不值得提到 headline 级?** 我们内部认为这可能是全项目技术含量最高的一个判断——它不是"我们实现了一个功能",而是"我们发现某个看似合法的动作在语义上必然破坏自身的验证前提,因此把它移出了执行序列"。但我们也怀疑这是自我感觉良好:它对评委而言可能只是一个抽象的时序问题。
2. **非专业评委能否在 30 秒内接住它?** 如果能,请给一句 40 字以内的中文说法(要求:不用 TOCTOU、快照、失效这类术语,但不许把逻辑讲错)。如果不能,请直接说不能,并说明它应该退到哪一层(附录?口头答辩备用?还是干脆不讲)。

---

## 口径提示(请你在建议里也遵守,避免替我们扩大化)

**"成对评测逐例完全一致(`n10 = n01 = 0`)"只对 TEST 与 BLIND 成立。**

- SYNTHETIC_TEST 2×2 = `19 / 0 / 0 / 3` —— 逐例完全一致;
- MANUAL_BLIND 2×2 = `31 / 0 / 0 / 14` —— 逐例完全一致;
- **DEV 2×2 = `63 / 1 / 1 / 13`** —— **含 1 例纠正与 1 例回归,并非逐例一致。**

因此"adapter 未改变任何一个判断"这句话,只能限定在 TEST/BLIND 上说,**不得推广到全部三个 split**。`NO_BENEFIT_EVIDENCE` 的结论不受影响(DEV 是 few-shot 策略选择集,且 +1/−1 净为零),但措辞必须准确。请你在给的任何呈现方案与话术里保持这个限定。

---

## 附录 A|我们自己纠出的三个错(请评价我们的纠错方法)

这一节不是求安慰。三个错**同属一类**:**事实存在,但地位不对。** 请据此判断我们的自查机制有没有系统性漏洞。

### 错误 1|数值标签串了(自查发现)

我们此前把 `0.3206965923309326 → 0.1786690652370453 = −44.3%` 称为"**训练损失**"。**错了。**

- **−44.3% 是固定探针交叉熵**(promoted fixed probe `s5_base_00bc9a0705e1b2873937`,396 targets,external selected-logit FP32 CE,adapter on/off delta = 15.140625,in-process 与 fresh unmerged reload 均 bit-exact);
- **训练损失**是 `0.5663164258003235 → 0.1900274008512497` = **−66.4%**。

两个数都真实,标签串了。已在全 lane 冻结改口径为"**固定探针交叉熵下降 44.3%**"。**附带的自我警告**:−66.4% 这个数本身也不该拿去讲——它是**首个 microstep 与末个 microstep 的单点 loss 之比**,不是 epoch 平均,54 次优化器更新的样本噪声完全可以主导它。我们倾向**两个数都不放进材料**,只讲"训练机制健康"(bit-exact 重载 / 12/12 gates / 梯度只流经 adapter)。请评价这个处置。

> 注:第三轮你曾正确纠正过我们一次数值口径("不说训练损失减半,实为 −44.3%")。这次的标签串位是我们自查出来的——但坦白说,是你那次纠正让我们去重查了这组数,所以这条不算纯自查。

### 错误 2|迁移"字节守恒但语义有损"(自查发现)

我们把两个几十万字节的状态 MD 迁到"**有界活跃快照 + 按日 journal**"两层结构(`state/v2/M2C_STATE.md` 12,209 B、`state/v2/QWEN_STATE.md` 14,988 B;只读归档 `/Users/gl/tzb/QWEN_BRAIN_STATE_ARCHIVE-20260830.md`,214,651 B,SHA-256 `942c0f9cc69240287ec32768e21e463a4cfa4461db3687f7f68041c76596bd94`,mode 0444)。

**迁移做了全量逐条字节守恒校验并通过**:181 / 110 条 legacy 记录逐条对 journal 比对,各仅缺 1 条(均为导入后新增),零丢失。

**但随后发现一个字节校验结构上查不出的缺陷**:§1–§4 核心区**原样照抄**(内容本就陈旧),其余全部降级为"§5 journal 缓存",而协议明文规定"**仅存在于 journal 的内容不是约束、不是待办、不是当前真相**"。于是一条既成事实——two-epoch 训练已完整 PASS(856 microsteps / 54 updates,12/12 gates)——因为**从没被提升进 §1**,被静默降权;lane owner 严格照协议办事,反而读到了陈旧的相反结论。

**最刺眼的物证**:该事实最终被提升进 §1 时,沿用了旧键名,于是权威状态里现在有一条:

```
- `final.not_started` — Pristine Node2 two-epoch PASS: 856 microsteps/54 updates, 12/12 gates; ...
```

**键名说"没开始",内容说"已 PASS 全绿"。** 字节守恒校验对此完全无感。

**我们已加的对策**:强制"**提升扫描**"——各 lane owner 在转只读前通读 journal,把该进核心区的事实提上来。**请评价:够吗?更一般地——如何验证一次迁移,才能覆盖这类"规范地位改变"缺陷?** 我们的直觉是需要一个"语义守恒"层面的校验(例如:迁移前后对同一组问题的可回答性必须一致),但不知道怎么把它做成可自动执行的门。

### 错误 3|已冻结的科学结论,其关键前提只存在于非规范记录中(写本咨询包时才发现)

`NO_BENEFIT_EVIDENCE` 早已被冻结为结论,`benefit_claim_authorized = false`,部署配置据此定为无 adapter。**但"被评的究竟是哪个 adapter"从未在权威状态的规范区记录过**,只散落在日志里。也就是说:一个已冻结的科学结论,其最关键的前提(评的是 two-epoch adapter,还是只跑了 32 microsteps 的 smoke adapter)当时**不可从规范区回答**。

这不是小事——**如果评的是 smoke adapter,`NO_BENEFIT_EVIDENCE` 基本不成立**,本咨询包的问题一就得整个重写。我们是在起草本文件、准备为这一点留一个"待填"占位时,才意识到必须去把它查实。查实结果见 §1.6:确系 two-epoch adapter,tree SHA `8a09f56a4a77c0127c0f1c2008efb8726e11d4a9b0c56f526a9934ce0a88f0b9`,与 smoke 的 publication SHA `b7ad8ebc4134f63165bad8b27be674b552c1bb83a39522ac6b9fb22be55f5d1c` 不同,DEV/TEST/BLIND 三处均显式绑定。**结论没变,但我们是靠运气而不是靠机制守住的。**

**请回答:一个已冻结的科学结论,其关键前提却只存在于非规范记录中,该用什么机制在冻结前强制暴露?** 我们能想到的是"结论冻结时必须同时冻结一张前提清单,清单每一项须指向规范区条目",但这看起来又会把治理往回加——而你第三轮刚判定治理已从资产转为负债、必须停止扩张。**这两者怎么平衡?**

### 对三者的元问题

三个错的共同结构是"**事实存在,但地位不对**":数字真实但标签错(错误 1)、记录完整但规范地位丢失(错误 2)、前提为真但不可从权威处回答(错误 3)。**这是巧合,还是我们的状态管理方式有一个共同的结构性缺陷?** 如果是后者,它是什么?

---

## 附录 B|本轮**不需要**你回答的(前三轮已裁定并落地,再问是浪费)

- **M2C 治理链机制**:预注册/冻结/分母/哈希绑定/事故不回填的具体做法,以及"最小充分集七项"。已裁定"停止扩张",材料中最多一页。
- **成对评测统计方法**:成对 2×2、exact McNemar、bootstrap CI、聚类数不足的标注(`UNSTABLE_LOW_CLUSTER_COUNT`)。已实现并冻结。
- **数据泄漏门设计**:感知哈希仪器有效性门、候选边 + 隔离元数据验证器确认硬边、连通分量原子分割。v4 已全程通过(495 源 / 573 行;验证器正样本召回 448/450;41,261 条 task-hard 负样本零误报)。
- **抓取坐标系假设的物理排查方案**:`hand → hand_tcp` 官方 103.4 mm 与实测 95.3 mm 的对照、2 小时自洽检查清单。方案已定,等窗口。

---

## 给你的总提示

1. **请直接说我们哪里想错了**,不要罗列可能性、不要综述。前三轮最有价值的都是你的反驳。
2. **优先级:问题一 > 问题二 ≈ 问题五 > 问题三 ≈ 问题四 > 问题六。** 问题一是本轮唯一无法回退的决定(叙事一旦对外定死就改不了);问题二与问题五都卡在 9/3 代码冻结前的最后改动窗口;问题三、四是材料期的事,9/3 后还能调;问题六最短。
3. 不要建议任何违反预注册协议、事后调阈值、挑选结果、隐藏阴性或伪造数据的做法。可以建议新的预注册实验和呈现策略。
4. 若你认为某条对策应当**放弃**,请直接说放弃,并给出替代。若你认为在 3.5 天工程 + 2 天材料的窗口里我们已经无法改善局面,也请直说,并给出"在既定局面下最大化得分"的方案。
5. 附录 A(我们自查的三个错)不需要你安慰,**需要你判断我们的自查机制有没有系统性漏洞**。
6. 注意"口径提示"一节:`n10 = n01 = 0` 只对 TEST/BLIND 成立,DEV 是 `63/1/1/13`。请不要在建议里把它推广到全部 split。
